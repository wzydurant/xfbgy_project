# 《稀饭拔根玉》Android 单机版技术方案

## 一、项目概述

### 1.1 游戏定位
回合制六角格兵棋游戏，以冷兵器为主、架空世界设定。玩家扮演指挥官，体验古代战争策略与内部分裂的双重挑战。

### 1.2 核心特色
- **双阵营对抗**：两个国家在六角格地图上进行回合制博弈
- **4-8人玩家支持**：支持联机与单机两种模式，玩家数量弹性配置
- **多角色指挥系统**：参谋长（全局后勤）与将军（野战机动）的双层指挥架构
- **野心家将军机制**：5人以上游戏中出现的隐藏叛变因子
- **威望与叛乱机制**：独特的内政威胁系统
- **丰富战斗维度**：炮击、远射、对攻、肉搏四种战斗类型
- **完整资源经济系统**：人力资源、装备、补给线
- **骑兵与牲畜系统**：牛/马区分，骑乘技能解锁骑兵移动力转化

### 1.3 玩家配置规则

| 玩家总数 | A方将军数 | B方将军数 | 野心家将军 |
|---------|---------|---------|-----------|
| 4人 | 1 | 1 | 无 |
| 5人 | 2 | 1（或1v2） | 多方1名 |
| 6人 | 2 | 2 | 各1名 |
| 7人 | 3 | 2（或2v3） | 多方1名 |
| 8人 | 3 | 3 | 各1名 |

> **规则**：双方各至少1名将军、1名参谋长。人数增加时增加将军名额。单数玩家时，多出将军的一方中有1名野心家将军；偶数玩家时（≥6人），双方各有1名野心家将军。4人局无野心家将军。

---

## 二、技术栈选型

### 2.1 框架与语言

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **跨平台框架** | Kotlin + Jetpack Compose | Android 首选，原生性能 |
| **替代方案** | Unity (C#) | 如需后期扩展到 iOS/PC |
| **语言版本** | Kotlin 1.9+ | 现代 Android 开发标准 |

### 2.2 核心依赖

```
// 核心框架
androidx.compose.ui          // UI 渲染
androidx.compose.material3    // Material Design 3
androidx.lifecycle           // 生命周期管理

// 数据持久化
Room Database                // 本地数据库存储游戏存档
DataStore                    // 偏好设置存储

// 游戏逻辑
Kotlinx Serialization        // JSON 序列化
Kotlinx Coroutines           // 异步处理与回合模拟

// 依赖注入
Hilt                         // 依赖注入框架

// 联机支持（可选模块）
Ktor (WebSocket / HTTP)      // 实时消息同步、房间管理
// 或 Firebase Realtime DB   // 备选：快速原型联机方案
```

### 2.2.1 联机架构设计

```
单机模式：
  本地 GameEngine ──→ 本地 StateFlow ──→ UI

联机模式（房主为服务端）：
  房主设备
    LocalGameEngine ──→ GameState ──→ WebSocketServer ──→ 广播给所有客户端
  
  客户端设备
    UI ──→ Command ──→ WebSocketClient ──→ 发送给房主
    WebSocketClient ──→ GameState同步 ──→ UI

命令验证规则：
  - 所有命令在房主端校验合法性后再广播
  - 客户端只负责渲染，不做本地预判（避免状态不一致）
  - 断线重连：客户端重连后请求完整 GameState 快照
```

### 2.3 架构模式：MVVM + Clean Architecture

```
┌─────────────────────────────────────────────┐
│                   UI Layer                   │
│  (Compose Screens, ViewModels)              │
├─────────────────────────────────────────────┤
│                 Domain Layer                 │
│  (Game Logic, Use Cases, Entities)           │
├─────────────────────────────────────────────┤
│                  Data Layer                  │
│  (Repositories, Room DB, DataStore)          │
└─────────────────────────────────────────────┘
```

### 2.4 状态管理方案
- **Compose State** + **StateFlow**：用于 UI 状态
- **Immutable Data Classes**：游戏状态不可变快照
- **Command Pattern**：命令模式处理单位操作

---

## 三、游戏数据模型

### 3.1 核心实体类

```kotlin
// ============ 角色系统 ============

enum class RoleType {
    CHIEF_OF_STAFF,   // 参谋长
    GENERAL,          // 将军
    TRAITOR_GENERAL   // 野心家将军（≥5人局中出现）
}

data class Character(
    val id: String,
    val name: String,
    val role: RoleType,
    val faction: Faction,
    val skills: List<Skill> = emptyList(),
    val authority: Int = 0,                      // 威望值
    val homeUnitId: String? = null,              // 本队单位ID（所在的自带单位）
    val subordinates: List<String> = emptyList() // 从属单位ID列表（不含本队）
)

/**
 * 本队规则：
 * - 每个将军/参谋长都有且仅有一支"本队"（其本人所在的单位）
 * - 本队下命令不占用命令配额（免费）
 * - 将军的命令数量上限仅针对本队以外的从属单位
 * - 将军自动进入第一个分配给自己的从属单位，该单位成为本队
 * - 参谋长初始本队 = 首都卫戍单位（游戏开始唯一单位）
 * - 建立新单位后可立刻在补给阶段调整从属；将军自动进入首个从属单位
 */

/**
 * 游戏初始状态：
 * - 每方仅有"首都卫戍"一个单位，位于首都格
 * - 参谋长位于此单位（本队 = 首都卫戍）
 * - 己方所有资源点自动归属本国（无需占领）
 * - 将军进入游戏后，需等待第一个从属单位建立
 */

enum class SkillType {
    // 将军技能
    FLAG_SIGNAL,          // 旗语: 指挥距离+2，指挥距离计算无视ZOC（将军本队移动不可无视ZOC）
    OFFICER_CORPS,        // 军官团: 命令数量8支
    WHITE_KNIGHT,         // 白骑士: 相邻友军士气+1、纪律+1（离开范围后消失）
    DEMON_KING,           // 魔王: 相邻敌军士气-1；己方昂首挺胸及以上士气单位纪律+1，萎靡不振及以下士气单位纪律-1（离开范围后消失）
    STORM_THE_FORMATION,  // 入阵讨: 斩杀将军效果
    GENIUS,               // 天才: 隐身伏击
                          //   - 指令阶段可指挥一个单位按正常规则移动后隐身（敌方不可见）
                          //   - 移动路径不可经过或进入敌军ZOC，否则立即现身、停止移动，只能发动常规打击
                          //   - 若敌军进入隐身单位ZOC：隐身单位现身，敌军立即停止移动且停止一切行动
                          //   - 隐身单位对触发敌军发动一轮打击（相邻→对攻，同一格→肉搏），该轮所有打击数值+1
                          //   - 若无敌军进入ZOC：补给阶段自动现身

    // 参谋长技能
    ALCHEMIST,            // 炼金术师: 生产火炮
    WISE_MINISTER,        // 贤相: 城市人力+1
    HORSE_ADMIN,          // 马政: 马场产量翻倍
    ASSASSIN,             // 鬼才: 刺杀己方将军
    SOLDIER_ORIGIN,       // 出身行伍: 2支从属，可野战
    TEACHER,              // 好为人师: 首都卫戍单位初始拥有所有单位技能

    // 单位技能（不可战场习得，只能由特定兵源地天生拥有或通过协助训练获得）
    RIDING,               // 骑乘: 解锁骑兵，马匹体力可转化为移动力
                          //   - 不是将军技能，无法在战场习得
                          //   - 获取方式1：少数特定资源点（如马场/特殊村庄）建立的单位天生拥有
                          //   - 获取方式2：与具备骑乘技能的单位处于同一格且双方均处于休整状态，协助训练获得
                          //   - 因补员失去骑乘技能的单位可自行训练恢复（无需其他单位协助）

    // 战场习得技能（属于单位技能，由单位自身通过行动习得，非将军技能）
    MOUNTAIN_COMBAT,      // 山地作战: 进入山地不散乱；山地恢复阵型时纪律视为+1(无上限)；山地间移动消耗1（进入山地仍为2）
    SHIELD_FORMATION,     // 盾阵
    ARTILLERY_IMMUNE      // 大炮也不过如此
}

data class Skill(
    val type: SkillType,
    val isActive: Boolean = true
)
```

### 3.2 单位系统

```kotlin
// ============ 单位属性 ============

enum class PersonnelLevel(val points: Int) {
    FULL(2),      // 满编: 2点
    REDUCED(1),   // 缺员: 1点
    REMNANT(0)    // 残部: 0点
}

enum class StaminaLevel(val value: Int) {
    MEAGER(3),        // 差强人意: 3
    ROBUST(4),         // 身强力壮: 4
    STRONG(5)          // 虎背熊腰: 5
}

enum class MoraleLevel(val attackMod: Int, val defenseMod: Int) {
    EXCITED(1, 0),    // 斗志昂扬
    STEADY(0, 0),     // 昂首挺胸
    DEPRESSED(-1, 0), // 萎靡不振
    PANICKED(-1, -1)  // 惊慌失措
}

enum class DisciplineLevel(val value: Int, val recoveryRate: Float) {
    MOB(1, 0.5f),         // 乌合之众: 50%
    DISCIPLINED(2, 0.667f), // 阵法不乱: 66.7%
    STRICT(3, 0.75f)      // 军纪森严: 75%
}

// ============ 单位状态 ============

/**
 * 状态分为安全（SAFE）和脆弱（FRAGILE）两大类：
 * - 安全状态：野战严整、野战散乱、守备 → 装备装备（实际生效）上限 ≤ 兵员体力
 * - 脆弱状态：行军、休整、运输、掠袭 → 装备装备（实际生效）上限 ≤ 1
 *
 * 携带装备（储备库存）不受状态限制，无上限；体力仅影响移动力计算。
 *
 * 守备特殊限制：
 *   - 进入条件：只有士气为昂首挺胸及以上（斗志昂扬/昂首挺胸）的单位才可进入守备状态
 *   - 最多持续3回合无战斗；第3回合结束后的补给阶段士气降为萎靡不振，随即强制转为休整状态
 *     （即萎靡不振无法维持守备，立刻切换为休整）
 *
 * 状态切换消耗命令，一回合只能执行一个命令（不可连续切换状态）。
 * 要退出某个状态，只能通过进入一个新的目标状态来实现，不存在"直接退出"命令。
 * 只有将军可下令进入野战状态。
 */

enum class UnitSafety { SAFE, FRAGILE }

enum class UnitState(val safety: UnitSafety, val maxEquippedWeight: Int) {
    FIELD_ORDERLY(UnitSafety.SAFE, -1),     // 野战(严整): 装备装备≤体力，仅将军可下令进入
    FIELD_DISORDERED(UnitSafety.SAFE, -1), // 野战(散乱): 装备装备≤体力，可被敌方进入
    GARRISON(UnitSafety.SAFE, -1),          // 守备: 装备装备≤体力，最多3回合无战斗→强制休整
    MARCH(UnitSafety.FRAGILE, 1),            // 行军: 装备装备≤1，用于移动
    REST(UnitSafety.FRAGILE, 1),             // 休整: 装备装备≤1，可训练/建工事/补员
    TRANSPORT(UnitSafety.FRAGILE, 1),        // 运输: 装备装备≤1，搭补给线，不可移动
    RAID(UnitSafety.FRAGILE, 1)              // 掠袭: 装备装备≤1，ZOC范围=相邻2格，可阻断夺取补给
}

// ============ 状态转换规则 ============

/**
 * 一回合只能执行一个命令，状态转换不能在同一回合内连续进行。
 *
 * 转换表：
 * 当前          → 目标           条件              成功率
 * 野战(散乱)    → 野战(严整)     无（含可在敌方ZOC内/肉搏中） 纪律/(纪律+1) × 堆叠修正
 * 野战(散乱)    → 休整           非敌方ZOC          100%
 * 休整          → 野战(严整)     无                 100%
 *
 * 其他转换（均消耗命令）：
 * 野战(严整)    → 守备（需士气≥昂首挺胸）/行军/休整/运输/掠袭
 * 守备          → 行军/休整/运输/掠袭
 * 行军          → 野战(严整)(仅将军)/休整/运输/掠袭
 * 休整          → 守备（需士气≥昂首挺胸）/行军/运输/掠袭
 * 运输          → 行军/休整/掠袭
 * 掠袭          → 行军/休整/运输
 *
 * 全局限制：只能在敌军 ZOC 外切换单位状态（阵型恢复例外）
 * 注：退出某状态 = 进入新目标状态，不存在独立的"退出"命令
 */
sealed class StateTransitionResult {
    data class Success(val newState: UnitState) : StateTransitionResult()
    data class Failed(val reason: String) : StateTransitionResult()
}

/**
 * 堆叠中重振修正：成功概率 = 基础成功率 × 0.8 / (同格总人力)
 * 重振失败无惩罚，可反复尝试（消耗命令）
 */
fun calculateRecoveryRateInStack(
    discipline: DisciplineLevel,
    totalPersonnelInHex: Int
): Float {
    val baseRate = discipline.recoveryRate
    return baseRate * 0.8f / totalPersonnelInHex
}

// ============ 装备系统 ============

/**
 * 装备负重表：
 *   长矛(1) | 剑(1) | 弓(1) | 盾牌(1) | 盔甲(1) | 火炮(4)
 *
 * 生产成本：
 *   长矛/剑/弓/盾牌 = 1装备点 | 盔甲 = 2装备点 | 火炮 = 2装备点(需炼金术师)
 *
 * 防御加成（仅「装备装备」生效）：
 *   盾牌: 对攻或肉搏时+1防御（不叠加，最多1个；远射不加成）
 *   盔甲: +1防御/件（无上限叠加，对远射/对攻/肉搏均生效）
 *
 * ══════════════════════════════════════════════════════════════════
 * 携带装备（储备库存） vs 装备装备（实际生效）
 * ══════════════════════════════════════════════════════════════════
 *
 * 【携带装备】CarriedEquipment — 单位库存中持有的全部装备：
 *   - 无数量/重量上限，单位想扛多少扛多少
 *   - 体力仅影响移动力计算（移动力 = 体力 - 携带装备总重量）
 *   - 携带装备总重量超过体力时，移动力可能为0或负数（不可移动）
 *   - 装备可在补给阶段沿补给线流通（资源点→单位、单位→单位）
 *     使用补给完粮食后仍有剩余容量的运输通道
 *
 * 【装备装备】EquippedGear — 单位当前实际穿戴、生效中的装备：
 *   - 受单位状态限制：
 *     安全状态(野战严整/野战散乱/守备)：装备装备总重量 ≤ 体力
 *     脆弱状态(行军/休整/运输/掠袭)：装备装备总重量 ≤ 1
 *   - 装备装备中的物品必须是携带装备中已有的（不能凭空装备）
 *   - 切换装备装备在将军/参谋长命令阶段进行（消耗命令）
 *   - 战斗计算仅使用装备装备，携带但未装备的不参与打击/防御
 *
 * 多武器规则：同一单位同时装备多种武器时，打击能力取各武器的最大值
 * 不装任何武器则不能发起打击（无武器=无打击能力）
 * 打击能力即使算出来为0，只要装备了对应武器就可以发起打击并掷骰判定
 */

enum class WeaponType(val weight: Int, val cost: Int) {
    SPEAR(1, 1),   // 长矛
    SWORD(1, 1),   // 剑
    BOW(1, 1)      // 弓
}

enum class DefensiveGearType(val weight: Int, val cost: Int) {
    SHIELD(1, 1),   // 盾牌: +1防御，不叠加
    ARMOR(1, 2)     // 盔甲: +1防御/件，无上限
}

data class ArtilleryGear(val weight: Int = 4, val cost: Int = 2)  // 火炮，需炼金术师

/**
 * 携带装备：单位库存中持有的全部装备（储备）
 * - 无数量/重量上限，想扛多少扛多少
 * - 体力仅影响移动力：移动力 = 体力 - 携带装备总重量
 * - 补给阶段可通过补给线输送（使用补完粮食后剩余的运输通道容量）
 */
data class CarriedEquipment(
    val weapons: List<WeaponType> = emptyList(),
    val shields: Int = 0,           // 最多1个
    val armors: Int = 0,            // 无上限
    val hasArtillery: Boolean = false
) {
    fun getTotalWeight(): Int {
        var w = weapons.sumOf { it.weight } + shields + armors
        if (hasArtillery) w += 4
        return w
    }
}

/**
 * 装备装备：单位当前实际穿戴、生效中的装备
 * - 数量不能超过携带装备（不能凭空装备）
 * - 受状态限制：
 *   安全状态(野战严整/野战散乱/守备)：总重量 ≤ 体力
 *   脆弱状态(行军/休整/运输/掠袭)：总重量 ≤ 1
 * - 战斗计算仅使用装备装备
 * - 在将军/参谋长命令阶段切换（消耗命令）
 */
data class EquippedGear(
    val weapons: List<WeaponType> = emptyList(),   // 装备中的武器（打击能力取最大值）
    val hasShield: Boolean = false,                 // 是否装备盾牌（最多1个）
    val armorCount: Int = 0,                        // 装备中的盔甲件数
    val hasArtillery: Boolean = false               // 是否装备火炮
) {
    fun getEquippedWeight(): Int {
        var w = weapons.sumOf { it.weight }
        if (hasShield) w += 1
        w += armorCount
        if (hasArtillery) w += 4
        return w
    }

    fun getDefenseBonus(): Int {
        val shieldBonus = if (hasShield) 1 else 0
        return shieldBonus + armorCount
    }

    /**
     * 按战斗类型计算防御加成：
     * - 盾牌：仅在对攻（ASSAULT）或肉搏（MELEE）时 +1，远射（ARCHERY）不加成
     * - 盔甲：对远射/对攻/肉搏均 +1/件
     */
    fun getDefenseBonusByCombatType(combatType: CombatType): Int {
        val shieldBonus = if (hasShield && (combatType == CombatType.ASSAULT || combatType == CombatType.MELEE)) 1 else 0
        return shieldBonus + armorCount
    }

    /**
     * 装备装备的打击能力（多武器取最大值）
     * 无武器则返回null（不能发起打击）
     */
    fun getCombatStats(isCavalry: Boolean): CombatStats? {
        if (weapons.isEmpty() && !hasArtillery) return null  // 无武器，不能发起打击

        var bestFormation = 0; var bestCasualty = 0; var bestMorale = 0
        var bestMeleeFormation = 0; var bestMeleeCasualty = 0; var bestMeleeMorale = 0

        for (weapon in weapons) {
            val stats = when (weapon) {
                WeaponType.SPEAR -> CombatStats(2, 1, 1, 1, 1, 0)
                WeaponType.SWORD -> CombatStats(1, 1, 1, 0, 3, 1)
                WeaponType.BOW -> CombatStats(1, 1, 1, 1, 1, 1)
            }
            bestFormation = maxOf(bestFormation, stats.assaultFormation)
            bestCasualty = maxOf(bestCasualty, stats.assaultCasualty)
            bestMorale = maxOf(bestMorale, stats.assaultMorale)
            bestMeleeFormation = maxOf(bestMeleeFormation, stats.meleeFormation)
            bestMeleeCasualty = maxOf(bestMeleeCasualty, stats.meleeCasualty)
            bestMeleeMorale = maxOf(bestMeleeMorale, stats.meleeMorale)
        }

        // 骑兵对攻阵型+1（山地除外，面对军纪森严除外）
        val cavalryBonus = if (isCavalry) 1 else 0
        return CombatStats(
            bestFormation + cavalryBonus, bestCasualty, bestMorale,
            bestMeleeFormation, bestMeleeCasualty, bestMeleeMorale
        )
    }
}

/**
 * 验证装备装备合法性：不能装备携带中没有的东西
 */
fun validateEquippedGear(carried: CarriedEquipment, equipped: EquippedGear): Boolean {
    // 武器：装备中的每种武器数量不能超过携带数量
    for (weapon in equipped.weapons) {
        val carriedCount = carried.weapons.count { it == weapon }
        val equippedCount = equipped.weapons.count { it == weapon }
        if (equippedCount > carriedCount) return false
    }
    // 盾牌：携带了才能装备
    if (equipped.hasShield && carried.shields == 0) return false
    // 盔甲：装备数不能超过携带数
    if (equipped.armorCount > carried.armors) return false
    // 火炮：携带了才能装备
    if (equipped.hasArtillery && !carried.hasArtillery) return false
    return true
}

// ============ 单位 ============

/**
 * 移动力公式：移动力 = 体力 - 装备重量
 * 体力来源：
 *   - 基础体力（StaminaLevel）
 *   - 牲畜体力：牛/马均提供 +2 体力，但：
 *       * 无骑乘技能：牲畜体力不转化为移动力，装备重量优先消耗牲畜体力
 *       * 有骑乘技能且有马：该单位可切换为骑兵状态，马提供的体力完全转化为移动力
 */

enum class LivestockType { OX, HORSE }

data class Livestock(
    val type: LivestockType,
    val staminaBonus: Int = 2  // 牛马均为+2体力
)

/**
 * 牲畜上限：一个单位最多同时拥有1牛和1马
 * 牲畜逃散规则：
 * - 触发条件：单位所在格有敌军 且 单位非野战严整状态
 * - 效果：牲畜逃散并带走单位装备清单中的全部武器和盔甲
 * - 豁免：若单位具备骑乘技能，则马不逃散（但牛仍逃散，装备仍被带走）
 * - 逃散后：牲畜和被带走的武器/盔甲均从单位移除，不可恢复
 */
data class LivestockFleeResult(
    val fledLivestock: List<Livestock>,    // 逃散的牲畜
    val lostWeapons: List<WeaponType>,     // 被带走的武器
    val lostShields: Int,                  // 被带走的盾牌
    val lostArmors: Int                    // 被带走的盔甲
)

fun checkLivestockFlee(unit: Unit, hasEnemyInSameHex: Boolean): LivestockFleeResult {
    if (!hasEnemyInSameHex) return LivestockFleeResult(emptyList(), emptyList(), 0, 0)
    if (unit.state == UnitState.FIELD_ORDERLY) return LivestockFleeResult(emptyList(), emptyList(), 0, 0)

    val hasRiding = unit.skills.any { it.type == SkillType.RIDING }
    // 有骑乘技能时马不逃散，牛仍逃散
    val fledLivestock = unit.livestockList.filter {
        !(hasRiding && it.type == LivestockType.HORSE)
    }

    // 逃散牲畜带走全部武器和盔甲
    return LivestockFleeResult(
        fledLivestock = fledLivestock,
        lostWeapons = unit.carriedEquipment.weapons,
        lostShields = unit.carriedEquipment.shields,
        lostArmors = unit.carriedEquipment.armors
    )
}

data class Unit(
    val id: String,
    val name: String,
    val commanderId: String?,         // 指挥官ID（将军或参谋长）
    val isHomeUnit: Boolean = false,  // 是否为指挥官本队
    val faction: Faction,
    val personnel: PersonnelLevel,
    val stamina: StaminaLevel,
    val morale: MoraleLevel,
    val discipline: DisciplineLevel,
    val state: UnitState,
    val carriedEquipment: CarriedEquipment,    // 携带装备（库存）
    val equippedGear: EquippedGear,             // 装备装备（当前穿戴，参与战斗计算）
    val position: HexCoord,
    val skills: List<Skill> = emptyList(),
    val fortificationLevel: Int = 0,   // 工事等级
    val fortificationProgress: Int = 0, // 建设进度
    val livestockList: List<Livestock> = emptyList(), // 牲畜列表（最多1牛1马）
    val isCavalry: Boolean = false,     // 是否处于骑兵状态（需骑乘技能+马）
    val garrisonTurns: Int = 0          // 守备持续回合数（超过3回合无战斗→强制休整）
) {
    /**
     * 实际移动力计算：
     * - 有骑乘技能且处于骑兵状态：移动力 = 体力(含马匹) - 装备重量
     * - 无骑乘技能或无马：移动力 = 体力(基础) - 装备重量
     *   （牲畜体力不加入移动力计算，但装备重量优先扣减牲畜体力）
     */
    fun calculateMovement(): Int {
        val baseStamina = stamina.value
        val livestockStamina = livestockList.sumOf { it.staminaBonus }
        val hasRiding = skills.any { it.type == SkillType.RIDING }
        val hasHorse = livestockList.any { it.type == LivestockType.HORSE }

        return if (hasRiding && hasHorse && isCavalry) {
            // 骑兵：全部体力（含马）参与移动力
            (baseStamina + livestockStamina) - carriedEquipment.getTotalWeight()
        } else {
            // 步兵：装备优先消耗牲畜体力，牲畜体力不转为移动力
            val weightAfterLivestock = maxOf(0, carriedEquipment.getTotalWeight() - livestockStamina)
            baseStamina - weightAfterLivestock
        }
    }

    /**
     * 溃散判定：
     * 士气为惊慌失措 且 非野战阵型严整 → 单位溃散，变为残部
     */
    fun checkRout(): Boolean {
        return morale == MoraleLevel.PANICKED && state != UnitState.FIELD_ORDERLY
    }

    /**
     * 白骑士/魔王"视为"士气等级：
     * 不仅是战斗判定，溃散判定也按视为后的等级
     */
    fun getEffectiveMorale(affectedByWhiteKnight: Boolean): MoraleLevel {
        if (affectedByWhiteKnight) {
            // 相邻友军士气视为提升一级（不超过最大等级）
            return when (morale) {
                MoraleLevel.PANICKED -> MoraleLevel.DEPRESSED
                MoraleLevel.DEPRESSED -> MoraleLevel.STEADY
                MoraleLevel.STEADY -> MoraleLevel.EXCITED
                MoraleLevel.EXCITED -> MoraleLevel.EXCITED  // 不超过最大
            }
        }
        return morale
    }

    /**
     * 兵力恢复时的装备丢失规则：
     *
     * 残部(0人力) → 恢复至缺员(1人力)或满编(2人力)：
     *   必定随机失去1件装备（火炮除外）
     *   优先从「装备装备」中随机选择，若无则从「携带装备」中随机选择
     *   同时同步移除装备装备中对应的装备（若丢失的是已装备的）
     *
     * 缺员(1人力) → 恢复至满编(2人力)：
     *   30%概率随机失去1件装备（火炮除外），70%概率不丢失
     *   优先从「装备装备」中随机选择，若无则从「携带装备」中随机选择
     *
     * 注：丢失的是「携带装备」中的物品，若丢失的恰好在「装备装备」中，
     * 则装备装备也需要同步移除
     */
}
data class EquipmentLossResult(
    val lostItem: String?,    // null表示未丢失，否则为丢失的装备描述
    val updatedCarried: CarriedEquipment,
    val updatedEquipped: EquippedGear
)

/**
 * 构建可丢失装备列表（不含火炮）
 */
fun buildLosableItemList(carried: CarriedEquipment): List<String> {
    val items = mutableListOf<String>()
    carried.weapons.forEach { items.add("weapon:${it.name}") }
    if (carried.shields > 0) items.add("shield")
    repeat(carried.armors) { items.add("armor") }
    // 火炮不纳入丢失列表
    return items
}

/**
 * 兵力恢复时判定装备丢失
 */
fun checkEquipmentLossOnRecovery(
    fromPersonnel: PersonnelLevel,
    toPersonnel: PersonnelLevel,
    carried: CarriedEquipment,
    equipped: EquippedGear
): EquipmentLossResult {
    val shouldLose = when (fromPersonnel) {
        PersonnelLevel.REMNANT -> true   // 残部恢复必定丢1件
        PersonnelLevel.REDUCED -> {
            if (toPersonnel == PersonnelLevel.FULL) {
                // 缺员→满编：30%概率丢失
                (1..100).random() <= 30
            } else false  // 缺员→残部（减员）不触发
        }
        PersonnelLevel.FULL -> false     // 满编不会恢复，不触发
    }

    if (!shouldLose) {
        return EquipmentLossResult(null, carried, equipped)
    }

    val losableItems = buildLosableItemList(carried)
    if (losableItems.isEmpty()) {
        return EquipmentLossResult(null, carried, equipped)  // 无可丢失装备
    }

    // 随机选择一件丢失
    val lostItem = losableItems.random()
    val updatedCarried = when {
        lostItem.startsWith("weapon:") -> {
            val weaponType = WeaponType.valueOf(lostItem.substringAfter(":"))
            val weaponList = carried.weapons.toMutableList()
            weaponList.remove(weaponType)
            carried.copy(weapons = weaponList)
        }
        lostItem == "shield" -> carried.copy(shields = carried.shields - 1)
        lostItem == "armor" -> carried.copy(armors = carried.armors - 1)
        else -> carried
    }

    // 同步移除装备装备中对应物品
    val updatedEquipped = when {
        lostItem.startsWith("weapon:") -> {
            val weaponType = WeaponType.valueOf(lostItem.substringAfter(":"))
            val equippedWeapons = equipped.weapons.toMutableList()
            equippedWeapons.remove(weaponType)
            equipped.copy(weapons = equippedWeapons)
        }
        lostItem == "shield" -> equipped.copy(hasShield = false)
        lostItem == "armor" -> equipped.copy(armorCount = maxOf(0, equipped.armorCount - 1))
        else -> equipped
    }

    return EquipmentLossResult(lostItem, updatedCarried, updatedEquipped)
}

// ============ 资源点 ============

/**
 * 资源产出规则：
 *   村庄: 每10回合1人力(储备), 每回合3粮食
 *   城镇: 每5回合1人力(储备), 每回合1粮食
 *   城市: 每回合1人力, 消耗1粮/回合, 每回合1装备; 被ZOC包围则停产
 *   马场: 每10回合1匹马（马政技能→5回合1匹）
 *
 * 贤相技能：城市人力产出+1（1→2）
 *
 * 资源储存与占领：
 * - 所有可储存资源（人力、装备、牲畜）被占领时：储存量减半，归占领方
 *
 * 据点压制规则：
 * - 进入敌方据点后进入压制状态，双方均无法获得资源
 * - 保持压制3回合后归属自动变更
 * - 压制期间原方进入 → 立即取消压制
 * - 野心家将军特权：针对本国资源点压制1回合即可占领（仅对叛乱前属己方的资源点）
 *
 * 多格城市的压制判定（特别说明）：
 * - 城市可能占据2-3个六角格（多格资源点）
 * - 压制成立条件：城市所有格均只有己方单位（无任何敌方单位）
 * - 只要有任意一格存在敌方单位，该城市整体不处于压制状态
 * - 原方进入任意一格 → 立即取消压制（无论其他格是否有己方单位）
 */
enum class ResourceType(val naturalFortLevel: Int) {
    VILLAGE(1),    // 村庄: 1格, 天生工事1级
    TOWN(2),       // 城镇: 1格, 天生工事2级
    CITY(3),       // 城市: 2-3格, 天生工事3级
    HORSE_RANCH(0) // 马场: 1格, 无工事
}

data class ResourceNode(
    val id: String,
    val type: ResourceType,
    val position: List<HexCoord>,  // 占据的格子（城市可为2-3格）
    val faction: Faction?,
    val stockpile: ResourceStockpile,
    val isSuppressed: Boolean = false,
    val suppressionTurns: Int = 0
)

/**
 * 多格城市压制状态判定
 * 压制成立条件：城市所有格均只有己方单位（无任何一格含敌方单位）
 */
fun ResourceNode.isSuppressedBy(suppressingFaction: Faction, getUnitsAt: (HexCoord) -> List<Unit>): Boolean {
    // 所有格都必须：(1) 有己方单位存在，(2) 无任何敌方单位
    return position.all { hex ->
        val units = getUnitsAt(hex)
        units.any { it.faction == suppressingFaction } && units.none { it.faction == faction }
    }
}

/**
 * 取消压制触发条件：原归属方进入城市任意一格
 */
fun ResourceNode.isSuppresionBrokenBy(originalFaction: Faction, getUnitsAt: (HexCoord) -> List<Unit>): Boolean {
    return position.any { hex ->
        getUnitsAt(hex).any { it.faction == originalFaction }
    }
}

data class ResourceStockpile(
    val personnel: Int = 0,
    val food: Int = 0,
    val equipment: Int = 0,
    val horses: Int = 0
)

/**
 * 装备生产（补给阶段）：
 * - 参谋长选择资源点下回合生产什么装备
 * - 有的装备需要持续生产2回合才能完成
 * - 生产消耗资源点的装备产出配额
 * - 处于压制状态的资源点不能组建军队
 */

/**
 * ══════════════════════════════════════════════════════════════════
 * 城市包围机制：
 * ══════════════════════════════════════════════════════════════════
 *
 * 包围定义：所有产粮资源点均无法通过一条不穿过敌方ZOC的路线进入城市（满足城市的粮食消耗）
 *   - 若存在至少一条如此路线，城市未被包围
 *   - 满足条件的路线：路线上的每格均不在敌方ZOC范围内（含敌方占领的格子）
 *
 * 未被包围时的运作：
 *   - 城市每回合消耗距其最近的未被封锁路线上的产粮资源点 1 单位粮食
 *
 * 被包围时的效果：
 *   - 城市本回合不消耗粮食
 *   - 城市本回合不生产装备和人力
 *   - 城市正常可用（守备、驻军等不受影响）
 *
 * 注：资源点处于压制状态时，亦不能组建军队（同上条规则）
 */
data class CitySupplyStatus(
    val cityId: String,
    val isBesieged: Boolean,            // 是否被包围
    val nearestFoodSourceId: String?,   // 最近的产粮路线资源点
    val pathClear: Boolean              // 补给路线是否畅通
)
data class ProductionOrder(
    val resourceNodeId: String,
    val targetEquipment: EquipmentType,
    val turnsCompleted: Int = 0,
    val turnsRequired: Int = 1  // 大部分1回合，特殊装备2回合
)

enum class EquipmentType {
    SPEAR, SWORD, BOW, SHIELD, ARMOR, CANNON
}

/**
 * 新兵团组建规则：
 * - 在有人力储存的资源点组建（补给阶段）
 * - 组建当回合不可移动、不可被吸收（非"已行动完毕"，而是组建在补给阶段后锁定）
 * - 下一回合开始可移动或吸收
 * - 常规新兵：差强人意/乌合之众/昂首挺胸/无技能
 * - 少数村庄可提供：身强力壮 或 骑乘技能
 *
 * ══════════════════════════════════════════════════════════════════
 * 补员规则（吸收单位兵力）：
 * ══════════════════════════════════════════════════════════════════
 *
 * 触发条件：双方处于同一格且都是休整状态，消耗接收方命令执行吸收
 * - 允许部分吸收（不必全部吸入）
 * - 同一被吸收单位可被多支部队分批吸收
 * - 最后一点人力被吸走后被吸收单位消失
 *
 * 【素质变化规则（恢复1级 vs 恢复2级）】
 * 恢复1级（如残部→缺员，缺员→满编）：
 *   - 有50%概率将接收单位的体力、士气、纪律重置为被吸收单位的对应属性
 *
 * 恢复2级（残部→满编）：
 *   - 有80%概率将接收单位的体力、士气、纪律重置为被吸收单位的对应属性
 *
 * 【技能变化规则（同上概率）】
 * 恢复1级：有50%概率失去本单位全部技能，改为习得被吸收单位的技能
 * 恢复2级：有80%概率失去本单位全部技能，改为习得被吸收单位的技能
 *   注：被吸收单位可能是训练后的单位，因此其技能可能有较高价值
 *
 * 【因补员失去技能的恢复途径】
 * 因补员失去技能（含骑乘技能等特殊技能）的单位，可自行训练来恢复该技能，无需其他单位协助
 *
 * ══════════════════════════════════════════════════════════════════
 * 补员带来的装备丢失规则（新规则，替代旧的按兵力恢复判定）：
 * ══════════════════════════════════════════════════════════════════
 *
 * 比较接收方与被吸收方的携带装备数量：
 *
 * 情形A：被吸收单位携带装备数 ≥ 接收单位携带装备数
 *   → 接收单位不会失去任何装备
 *
 * 情形B：被吸收单位携带装备数 < 接收单位携带装备数
 *   → 装备差值 = 接收方携带装备数 - 被吸收方携带装备数
 *   → 每次判定失去概率 = 装备差值 / (装备差值 + 1)
 *   → 若判定失去：随机失去一件装备（火炮除外）
 *   → 然后重新计算新差值，继续判定，直至判定失败或无装备可失去
 *   → 丢失装备同时从携带装备和装备装备中移除
 */
data class AbsorptionQualityResult(
    val qualityChanged: Boolean,   // 是否发生素质/技能变化
    val newStamina: StaminaLevel,
    val newMorale: MoraleLevel,
    val newDiscipline: DisciplineLevel,
    val newSkills: List<Skill>
)

/**
 * 补员素质变化判定
 * @param recoveryLevels 恢复的兵力等级数（1 或 2）
 */
fun checkAbsorptionQualityChange(
    receiver: Unit,
    donor: Unit,
    recoveryLevels: Int
): AbsorptionQualityResult {
    val changeProb = if (recoveryLevels >= 2) 0.8f else 0.5f
    val changed = (0f..1f).random() < changeProb
    return if (changed) {
        AbsorptionQualityResult(true, donor.stamina, donor.morale, donor.discipline, donor.skills)
    } else {
        AbsorptionQualityResult(false, receiver.stamina, receiver.morale, receiver.discipline, receiver.skills)
    }
}

/**
 * 补员装备丢失连续判定
 * @param receiverCarriedCount 接收方携带装备总数
 * @param donorCarriedCount    被吸收方携带装备总数
 */
fun calculateAbsorptionEquipmentLoss(
    receiverCarriedCount: Int,
    donorCarriedCount: Int,
    carried: CarriedEquipment,
    equipped: EquippedGear
): EquipmentLossResult {
    if (donorCarriedCount >= receiverCarriedCount) {
        return EquipmentLossResult(null, carried, equipped)  // 被吸收方装备不少，不丢失
    }

    var currentCarried = carried
    var currentEquipped = equipped
    var diff = receiverCarriedCount - donorCarriedCount
    var lastLost: String? = null

    while (diff > 0) {
        val lossProb = diff.toFloat() / (diff + 1)
        if ((0f..1f).random() >= lossProb) break  // 判定失败，停止丢失

        val losableItems = buildLosableItemList(currentCarried)
        if (losableItems.isEmpty()) break

        val lostItem = losableItems.random()
        lastLost = lostItem
        val lossResult = applySingleEquipmentLoss(lostItem, currentCarried, currentEquipped)
        currentCarried = lossResult.updatedCarried
        currentEquipped = lossResult.updatedEquipped
        diff--  // 差值减少，继续判定
    }

    return EquipmentLossResult(lastLost, currentCarried, currentEquipped)
}

/**
 * 执行单件装备丢失
 */
fun applySingleEquipmentLoss(
    lostItem: String,
    carried: CarriedEquipment,
    equipped: EquippedGear
): EquipmentLossResult {
    val updatedCarried = when {
        lostItem.startsWith("weapon:") -> {
            val weaponType = WeaponType.valueOf(lostItem.substringAfter(":"))
            val weaponList = carried.weapons.toMutableList(); weaponList.remove(weaponType)
            carried.copy(weapons = weaponList)
        }
        lostItem == "shield" -> carried.copy(shields = carried.shields - 1)
        lostItem == "armor" -> carried.copy(armors = carried.armors - 1)
        else -> carried
    }
    val updatedEquipped = when {
        lostItem.startsWith("weapon:") -> {
            val weaponType = WeaponType.valueOf(lostItem.substringAfter(":"))
            val equippedWeapons = equipped.weapons.toMutableList(); equippedWeapons.remove(weaponType)
            equipped.copy(weapons = equippedWeapons)
        }
        lostItem == "shield" -> equipped.copy(hasShield = false)
        lostItem == "armor" -> equipped.copy(armorCount = maxOf(0, equipped.armorCount - 1))
        else -> equipped
    }
    return EquipmentLossResult(lostItem, updatedCarried, updatedEquipped)
}

```

### 3.3 地图系统

```kotlin
// ============ 六角格坐标系统 ============

data class HexCoord(val q: Int, val r: Int) {
    // 六角格邻居计算
    fun getNeighbors(): List<HexCoord> = listOf(
        HexCoord(q + 1, r),     // 东
        HexCoord(q + 1, r - 1), // 东北
        HexCoord(q, r - 1),     // 西北
        HexCoord(q - 1, r),     // 西
        HexCoord(q - 1, r + 1), // 西南
        HexCoord(q, r + 1)      // 东南
    )

    // 六角格距离计算 (Cube coordinates)
    fun distanceTo(other: HexCoord): Int {
        return ((kotlin.math.abs(q - other.q) +
                 kotlin.math.abs(q + r - other.q - other.r) +
                 kotlin.math.abs(r - other.r)) / 2)
    }
}

// ============ 地形系统 ============

enum class TerrainType {
    PLAIN,      // 平地: 移动1, 视野正常
    MOUNTAIN,   // 山地: 移动2, 视野-1, 防御+1
    RIVER,      // 河流: 移动2(需渡口), 跨河攻击-2
    BUILDING,   // 建筑群: 移动1, 工事等级=建筑类型
    IMPASSABLE  // 不可进入
}

data class HexTile(
    val coord: HexCoord,
    val terrain: TerrainType,
    val resourceNode: ResourceNode? = null,
    val unit: Unit? = null,
    val fortification: Int = 0  // 人工工事
)

// ============ 地图数据 ============

data class GameMap(
    val width: Int,
    val height: Int,
    val tiles: Map<HexCoord, HexTile>,
    val capitals: Map<Faction, HexCoord>  // 双方首都位置
)
```

### 3.4 战斗系统

```kotlin
// ============ 战斗数据 ============

enum class CombatType {
    ARTILLERY,  // 炮击: 1-5格, 打散阵型
    ARCHERY,    // 远射: 相隔1格, 移动力-1
    ASSAULT,    // 对攻: 相邻格
    MELEE       // 肉搏: 同格
}

data class CombatStats(
    val assaultFormation: Int,
    val assaultCasualty: Int,
    val assaultMorale: Int,
    val meleeFormation: Int,
    val meleeCasualty: Int,
    val meleeMorale: Int
)

/**
 * 打击结算规则：
 * 阵型打击、减员打击、士气打击三项同时独立结算，效果可同时产生。
 * 每项打击独立判定成功（打击能力 + 士气修正 + 骰子 ≥ 防御力）。
 *
 * 阵型打击结算细节：
 * - 若打击成功且对方处于野战严整 → 对方变为野战散乱
 * - 若打击成功且对方本身不是野战严整 → 对方纪律下降1级（最低降至乌合之众，不消灭）
 *
 * 减员打击结算细节：
 * - 减员打击成功使对方兵力等级下降1级（满编→缺员→残部→消灭）
 * - 注意：对方有工事时，防御力叠加工事防御掷骰结果（工事骰与兵力防御修正叠加计算）
 *
 * 打击目标选择规则（目标格内有多个敌方单位时）：
 * - 优先打击非野战严整（散乱/脆弱状态）的单位
 * - 若有多个非严整单位，由进攻方选择打击其中一个
 * - 若所有单位均为野战严整，由防守方选择一个单位受击
 *
 * 残部强制散乱：残部若处于野战状态，强制处于阵型散乱，不可重振
 */
data class CombatResult(
    val formationSuccess: Boolean,    // 阵型打击是否成功
    val casualtySuccess: Boolean,     // 减员打击是否成功
    val moraleSuccess: Boolean,       // 士气打击是否成功
    val formationDamage: Int,         // 实际阵型打击量
    val casualtyReduction: Int,       // 实际减员量
    val moraleDamage: Int,            // 实际士气打击量
    val eliminated: Boolean = false
)

// ============ 战斗公式 ============

/**
 * 打击发起条件：
 * - 只要装备了对应武器即可发起打击，即使最终打击能力算出来为0也可以掷骰
 * - 没有装备任何武器则不能发起打击
 * - 打击计算仅使用「装备装备」(EquippedGear)，携带但未装备的不参与
 *
 * 打击成功条件：打击能力 + 士气修正 + 骰子点数 ≥ 防御力
 * 骰子：六面骰（1-6）
 *
 * 兵力骰规则（发起打击时）：
 *   满编(2人力) → 1颗骰（基础）
 *   缺员(1人力) → 2颗骰，取较小值
 *   残部(0人力) → 3颗骰，取较小值
 * 兵力越少，骰子越多但取小，反映组织度下降的不稳定性
 *
 * 士气修正：斗志昂扬+1，萎靡不振-1，惊慌失措-1
 *
 * 防御力 = 基础防御力 + 装备防御加成(装备装备) + 兵力修正 + 士气防御修正
 *   基础防御力：野战(严整)/守备 → 5；其他 → 4
 *   装备防御加成：盾牌+1(不叠加)，盔甲+1/件
 *   兵力修正：满编0，缺员-1，残部-2（基于当前人力等级，与基础防御力叠加）
 *   士气防御修正：惊慌失措-1，其余0
 *
 * 肉搏散乱减益（非野战严整的一切状态在肉搏中）：
 *   打击能力 -1，防御力 -1（最低0）
 * 散乱状态肉搏：每回合额外降低1级士气
 */

/**
 * 远射机制（弓，相隔1格，需装备弓）：
 * - 移动力打击能力1（仅本回合有效）
 * - 命中后对方打击能力也下降1（仅本回合有效）
 * - 不造成伤亡
 */

/**
 * 对攻附加规则：
 * - 骑兵对攻阵型打击额外+1（山地地形除外，面对军纪森严除外）
 *
 * 对攻后进入敌方格（冲锋）：
 *   条件1（普通）：对攻阵型打击成功，且对方格内不存在其他阵型严整单位
 *     → 进攻方可选择直接进入对方六角格
 *     → 若进攻方为骑兵，可免费发起一轮肉搏打击
 *   条件2（工事内守备）：对方为工事守备状态
 *     → 只有对攻阵型打击成功时，进攻方才可选择进入工事
 *     → 进入工事后，守备单位不再享受工事防御增益
 *     → 肉搏时守备状态视为阵型散乱（承受散乱减益）
 */

/**
 * 炮击（需装备火炮）：
 * - 距离最多5格，中间不得有部队遮挡
 * - 成功率 = 1 / (距离 + 1)，首次被炮击 = 2 / (距离 + 2)
 * - 炮击成功：阵型-1
 * - 炮击首先击毁该朝向工事，再打击守备部队
 * - "大炮也不过如此"技能生效后：炮击成功不再降低士气
 */

/**
 * 基础防御力（仅与状态相关）
 */
fun getBaseDefense(unit: Unit): Int = when (unit.state) {
    UnitState.FIELD_ORDERLY, UnitState.GARRISON -> 5
    else -> 4
}

/**
 * 兵力防御修正（叠加在基础防御力上）
 * 满编(2人力) → 0，缺员(1人力) → -1，残部(0人力) → -2
 */
fun getPersonnelDefenseModifier(personnel: PersonnelLevel): Int = when (personnel) {
    PersonnelLevel.FULL -> 0
    PersonnelLevel.REDUCED -> -1
    PersonnelLevel.REMNANT -> -2
}

/**
 * 完整防御力计算
 * = 基础防御力 + 装备防御加成(装备装备) + 兵力修正 + 肉搏散乱减益 + 士气防御修正
 */
fun calculateFullDefense(
    unit: Unit,
    equipped: EquippedGear,
    isMelee: Boolean
): Int {
    val base = getBaseDefense(unit)
    val equipmentBonus = equipped.getDefenseBonus()
    val personnelMod = getPersonnelDefenseModifier(unit.personnel)
    val meleePenalty = if (isMelee && unit.state != UnitState.FIELD_ORDERLY) 1 else 0
    val moralePenalty = if (unit.getEffectiveMorale(affectedByWhiteKnight = false) == MoraleLevel.PANICKED) 1 else 0

    return maxOf(0, base + equipmentBonus + personnelMod - meleePenalty - moralePenalty)
}

/**
 * 打击骰数量（基于当前人力等级）
 * 满编 → 1颗，缺员 → 2颗取小，残部 → 3颗取小
 */
fun getAttackDiceCount(personnel: PersonnelLevel): Int = when (personnel) {
    PersonnelLevel.FULL -> 1
    PersonnelLevel.REDUCED -> 2
    PersonnelLevel.REMNANT -> 3
}

/**
 * 掷骰：根据兵力等级掷出相应数量的骰子
 * 1颗 → 直接取值；多颗 → 取最小值
 */
fun rollAttackDice(personnel: PersonnelLevel): Int {
    val count = getAttackDiceCount(personnel)
    return (1..count).map { (1..6).random() }.minOrNull() ?: 1
}
```

### 3.5 ZOC 与状态切换规则

```kotlin
// ============ ZOC（控制区）规则 ============

/**
 * ZOC 范围：
 * - 野战严整单位（未进入肉搏）：相邻格（6格）+ 本格
 * - 守备单位（未进入肉搏）：相邻格（6格）+ 本格
 * - 掠袭单位：相邻2格（共12格+本格）
 * - 野战散乱单位：仅本格（散乱单位有本格ZOC）
 * - 其他所有状态（行军、休整、运输、肉搏中）：仅本格
 * - 残部（野战散乱状态）：仍有本格ZOC（散乱规则）
 *
 * 特殊规则：
 * - 敌我 ZOC 可在同一格叠加，该格同时属于双方 ZOC
 * - 散乱单位有本格 ZOC（并非无ZOC）
 * - 单位移动进入敌方 ZOC 须立刻停止本回合移动
 *   例外：掠袭状态单位的 ZOC 不能阻止敌军移动（掠袭只用于补给阻断）
 *
 * 旗语技能说明：
 * - 将军指挥距离+2，且计算指挥距离时路线可无视ZOC（不必走无ZOC路线）
 * - 将军本队自身移动不可无视ZOC（本队移动同样受ZOC停止规则约束）
 * - 旗语技能的"无视ZOC"仅指挥距离路线计算，不赋予移动豁免
 * - 有旗语将军的情况下，其从属单位接收命令的路线判定也可无视ZOC（指挥距离从5扩展为7格）
 */

enum class ZocType { ADJACENT_1, ADJACENT_2, SELF_ONLY, NONE }

fun getZocRange(unit: Unit): ZocType = when {
    unit.state == UnitState.FIELD_ORDERLY -> ZocType.ADJACENT_1
    unit.state == UnitState.GARRISON && !unit.isInMelee -> ZocType.ADJACENT_1
    unit.state == UnitState.RAID -> ZocType.ADJACENT_2       // 掠袭：相邻2格（仅补给阻断，不阻止移动）
    unit.personnel == PersonnelLevel.REMNANT -> ZocType.SELF_ONLY  // 残部：仍有本格ZOC
    else -> ZocType.SELF_ONLY
}

// ============ 状态切换规则 ============

/**
 * 状态切换限制：
 * - 只有在敌军 ZOC 外（即该格不受任何敌军 ZOC 覆盖）才可切换单位状态
 * - 包括：野战严整↔散乱、切换为行军/休整/守备等
 * - 例外：野战重整阵型可在敌方ZOC内/肉搏中执行
 */

// ============ 堆叠与散乱规则 ============

/**
 * 多单位堆叠触发散乱判定：
 * - 触发时机：进入含有友方单位或敌方单位的六角格时
 * - 判定双方：进入方与已在格内的单位双方均需判定
 * - 散乱概率公式：与进入城市相同 = 1 / (3 × 纪律等级)
 * - 例外：进入空格不触发此判定
 * - 注意：城市建筑群进入依然触发进入城市的散乱判定（叠加适用）
 */

/**
 * 离开多单位堆叠格限制：
 *
 * 情形A：格内同时存在敌方单位（肉搏状态）
 *   - 阵型严整（FIELD_ORDERLY）的一方可主动离开；受肉搏压制的另一方无法重振阵型
 *   - 阵型散乱的一方受压制，无法重振（直至己方格内出现严整单位或对方严整单位离开/被消灭）
 *
 * 情形B：格内全为己方单位
 *   - 离开需进行判定：成功概率 = 离开单位纪律等级 / (本格单位数 + 离开单位纪律等级)
 *   - 判定成功：可离开
 *   - 判定失败：本回合命令阶段不可离开，且该单位本回合命令阶段不可执行其他任何行动
 */

// ============ 阵型恢复（野战重振）堆叠减益 ============

/**
 * 野战重整时的有效纪律等级：
 * 有效纪律 = max(1, 原纪律 - (同格非野战严整友军数 - 1) - 同格敌军数)
 * 注：只有非野战严整的友军（散乱/脆弱状态）才纳入惩罚计算
 */
fun calculateEffectiveDisciplineForRecovery(
    originalDiscipline: DisciplineLevel,
    nonOrderlyFriendlyUnitsInHex: Int,
    enemyUnitsInHex: Int
): DisciplineLevel {
    val penalty = (nonOrderlyFriendlyUnitsInHex - 1) + enemyUnitsInHex
    val effectiveValue = maxOf(1, originalDiscipline.value - penalty)
    return when (effectiveValue) {
        1 -> DisciplineLevel.MOB
        2 -> DisciplineLevel.DISCIPLINED
        else -> DisciplineLevel.STRICT
    }
}
```

### 3.5 补给系统

```kotlin
// ============ 补给线与流量机制 ============

/**
 * 运输单位：处于运输状态的单位，不可移动。
 * 输送能力 = 运输单位剩余体力 × 4
 * 剩余体力 = 兵员体力 + 牲畜体力 - 携带装备重量
 * 实际输送量 = 输送能力 - 运输单位自耗粮食
 *
 * 运输单位自耗：
 *   满编(2人力) = 2粮食/回合
 *   缺员(1人力) = 1粮食/回合
 *   残部(0人力) = 0粮食/回合（残部不消耗粮食，但断补仍生效）
 *
 * 传递规则：
 * - 传递范围 = ceil(运输单位移动力 / 2) 格
 * - 不要求下一个运输单位在此范围内
 * - 但补给线会被敌方 ZOC 阻断
 * - 整条补给线受最薄弱运输单位限制（瓶颈效应）
 * - 粮食和储备装备共用同一输送能力
 *
 * 储备装备输送规则（补给阶段）：
 * - 只能使用补给完粮食后仍有剩余容量的运输通道
 * - 可从资源点储备输送至目标单位（加入其携带装备）
 * - 也可从一个单位输送至另一个单位（携带装备间转移）
 * - 输送方向：沿着补给线路径，从后方→前方
 *
 * 牲畜输送（补给阶段）：
 * - 牛/马可沿补给线输送至前线单位
 * - 不消耗运输通道流量（与粮食/装备独立）
 * - 补给阶段装备完毕，下一命令阶段生效
 */

data class TransportUnit(
    val unitId: String,
    val capacityPerTurn: Int,       // 输送能力
    val selfConsumption: Int,       // 自耗粮食
    val effectiveCapacity: Int,     // 实际输送量 = capacity - selfConsumption
    val transferRange: Int          // 传递范围 = ceil(movement / 2)
)

/**
 * 断补规则：
 * 补给不足时，离运输状态单位最远的部队优先断补。
 */
enum class SupplyBreakStage {
    NORMAL,          // 正常
    FIRST_TURN,      // 第1回合断补：无法炮击和远射
    SECOND_TURN,     // 第2回合断补：若有牲畜则消耗牲畜（不降士气）；若无牲畜则直接降低士气
    THIRD_TURN_PLUS  // 第3+回合：持续降低士气；惊慌失措后每回合判定溃散
}

/**
 * 断补第2回合处理逻辑：
 * if (unit.livestockList.isNotEmpty()) {
 *     unit = unit.copy(livestockList = emptyList())  // 消耗所有牲畜，本回合不降士气
 * } else {
 *     unit = unit.copy(morale = unit.morale.decrease())  // 无牲畜直接降士气
 * }
 */

/**
 * 掠袭获取补给：
 * 掠袭状态单位可阻断敌方补给源或运输部队，夺取被阻断补给量的50%（向下取整）
 */
fun calculateRaidCapture(blockedSupply: Int): Int = blockedSupply / 2

data class SupplyStatus(
    val unitId: String,
    val isSupplied: Boolean,
    val breakStage: SupplyBreakStage,
    val distanceFromSupply: Int
)
```

---

## 四、回合流程设计

### 4.1 回合结构

```
┌──────────────────────────────────────────────────────┐
│                    回合 N 开始                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │           阶段1: 将军命令阶段                   │ │
│  │  A国1将军 → B国1将军 → A国2将军 → B国2将军 → ... │ │
│  │  • 移动                                          │ │
│  │  • 切换状态                                      │ │
│  │  • 战斗命令                                      │ │
│  │  • 重振阵型                                      │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │           阶段2: 参谋长命令阶段                 │ │
│  │  A国参谋长 → B国参谋长                          │ │
│  │  • 移动未行动单位                                │ │
│  │  • 训练、建设、补员                             │ │
│  │  • 资源调配                                     │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │           阶段3: 补给阶段                       │ │
│  │  ① 资源产出结算（粮食/人力/装备/马匹）          │ │
│  │  ② 根据产出粮食判定各部队补给状态               │ │
│  │  ③ 双方参谋长交替组建新兵团（A→B→A→B...）     │ │
│  │  ④ 双方参谋长交替调整装备                       │ │
│  │     （参谋长可从资源点/非将军从属单位→将军从属） │ │
│  │     （不可让将军从属单位的装备向其他地方转移）   │ │
│  │  ⑤ 双方将军交替调整装备                         │ │
│  │     （将军可从其从属单位→任意单位或资源点转移）  │ │
│  │  ⑥ 牛/马沿补给线输送（不消耗运输通道流量）      │ │
│  │  ⑦ 参谋长为各资源点指定下回合生产装备            │ │
│  │  ⑧ 压制判定（3回合→归属变更/原方进入→取消）    │ │
│  │  ⑨ 断补状态更新（最远优先断补）                 │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
│  ┌────────────────────────────────────────────────┐ │
│  │           阶段4: 回合结算                        │ │
│  │  • 士气自动下降判定（肉搏中非严整每回合-1）      │ │
│  │    （目前无士气自动恢复机制，只能通过训练恢复）  │ │
│  │  • 守备超时判定（3回合无战斗→降士气→强制休整）  │ │
│  │  • 溃散判定（惊慌失措+非严整→残部）             │ │
│  │  • 胜利条件判定                                 │ │
│  └────────────────────────────────────────────────┘ │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 4.2 命令系统

```kotlin
// ============ 可用命令 ============

sealed class Command {
    // 移动命令
    data class Move(val unitId: String, val path: List<HexCoord>) : Command()

    // 状态切换
    data class ChangeState(val unitId: String, val newState: UnitState) : Command()

    // 装备切换（携带装备→装备装备，在将军/参谋长命令阶段消耗命令切换）
    data class ChangeEquipment(val unitId: String, val newEquippedGear: EquippedGear) : Command()

    // 战斗命令
    data class Artillery(val attackerId: String, val targetId: String) : Command()
    data class Archery(val attackerId: String, val targetId: String) : Command()
    data class Assault(val attackerId: String, val targetId: String) : Command()

    // 阵型恢复
    data class RestoreFormation(val unitId: String) : Command()

    // 休整行动
    data class Train(val unitId: String, val attribute: String) : Command()
    data class Absorb(val receiverId: String, val donorId: String) : Command()
    data class BuildFort(val unitId: String, val direction: Int) : Command()

    // 指挥官特殊命令
    data class TransferCommand(val commanderId: String, val targetUnitIds: List<String>) : Command()

    // 装备生产（补给阶段）
    data class OrderProduction(val resourceNodeId: String, val equipment: EquipmentType, val turnsRequired: Int = 1) : Command()

    // 叛乱死士部署（仅野心家将军，任意时刻均可部署）
    data class DeployAssassin(val generalId: String, val position: HexCoord, val carriedEquipment: CarriedEquipment, val equippedGear: EquippedGear) : Command()

    // 发动叛乱（野心家将军随时可触发）
    data class StartRebellion(val generalId: String) : Command()
}

// ============ 命令限制 ============

data class CommandLimits(
    val maxOtherUnitCommands: Int = 6,  // 普通将军
    val commandRange: Int = 5,          // 普通指挥范围
    val canEnterFieldState: Boolean = false  // 参谋长不可
) {
    companion object {
        fun forGeneral(skills: List<Skill>): CommandLimits {
            val hasFlagSignal = skills.any { it.type == SkillType.FLAG_SIGNAL }
            val hasOfficerCorps = skills.any { it.type == SkillType.OFFICER_CORPS }
            return CommandLimits(
                maxOtherUnitCommands = if (hasOfficerCorps) 8 else 6,
                commandRange = if (hasFlagSignal) 7 else 5
            )
        }

        val forChiefOfStaff = CommandLimits(
            maxOtherUnitCommands = Int.MAX_VALUE,  // 全图
            commandRange = Int.MAX_VALUE,
            canEnterFieldState = false
        )
    }
}
```

### 4.3 特殊移动与状态规则

#### 4.3.1 进入建筑群散乱规则
```kotlin
/**
 * 触发条件：野战状态（FIELD_ORDERLY 或 FIELD_DISORDERED）的单位进入建筑群时
 * - 只有野战状态才触发（行军/休整/运输/掠袭/守备等状态进入不触发）
 * - 散乱概率 = 纪律等级 / (纪律等级 + 1)
 *   乌合之众(1): 50%    阵法不乱(2): 66.7%    军纪森严(3): 75%
 *
 * 若本身已是阵型散乱状态进入建筑群，则士气下降1级（不再判定散乱）
 * 纪律下降不低于乌合之众，士气下降不低于惊慌失措
 */
enum class DisciplineLevel(val value: Int, val recoveryRate: Float) {
    MOB(1, 0.5f),           // 乌合之众: 1/3 ≈ 33.3%
    DISCIPLINED(2, 0.667f), // 阵法不乱: 1/6 ≈ 16.7%
    STRICT(3, 0.75f)        // 军纪森严: 1/9 ≈ 11.1%
}

fun calculateBuildingEntryDisorderProb(discipline: DisciplineLevel): Float {
    return discipline.value.toFloat() / (discipline.value + 1)
}

// 示例:
// 乌合之众进入建筑群: 50% 概率散乱
// 阵法不乱进入建筑群: 66.7% 概率散乱
// 军纪森严进入建筑群: 75% 概率散乱
```

#### 4.3.2 进入含有野战严整单位的六角格散乱规则
```kotlin
/**
 * 触发条件：野战状态（严整）的单位进入（或经过）含有其他野战严整单位的六角格时
 * - 只有双方均为野战严整状态时才触发（其他状态单位不触发此判定）
 * - 进入方与格内每支野战严整单位均需独立判定是否散乱
 * - 进入建筑群同时也触发建筑群散乱判定（两条独立叠加判定）
 *
 * 散乱概率 = 纪律等级 / (纪律等级 + 1)
 *   乌合之众(1): 50%    阵法不乱(2): 66.7%    军纪森严(3): 75%
 */
fun calculateHexEntryDisorderProb(discipline: DisciplineLevel): Float {
    return discipline.value.toFloat() / (discipline.value + 1)
}
```

#### 4.3.3 离开多单位堆叠格限制
```kotlin
/**
 * 情形A：格内同时存在敌方单位（肉搏状态）
 *   - 只有阵型严整（FIELD_ORDERLY）的单位可主动离开
 *   - 受肉搏压制的散乱方无法重振阵型
 * 情形B：格内全为己方单位
 *   - 离开需进行判定：p = 纪律等级 / (本格单位数 + 纪律等级)
 *   - 判定失败：本回合不可离开且不可执行其他行动
 */
fun canLeaveStackedHex(unit: Unit, hasEnemyInHex: Boolean): Boolean {
    // 存在敌方单位（肉搏）：只有严整才能离开
    if (hasEnemyInHex) return unit.state == UnitState.FIELD_ORDERLY
    // 全是己方：还需骰子判定（此处仅检查基础前提条件为严整）
    return unit.state == UnitState.FIELD_ORDERLY || unit.state == UnitState.GARRISON
}

/**
 * 全友军堆叠离开判定概率
 * p = 纪律等级 / (本格单位数 + 纪律等级)
 */
fun calculateLeaveStackProbability(discipline: DisciplineLevel, totalUnitsInHex: Int): Float {
    return discipline.value.toFloat() / (totalUnitsInHex + discipline.value)
}
```

#### 4.3.4 ZOC 与状态切换
```kotlin
/**
 * ZOC（控制区）范围规则：
 * - 野战严整单位（未进入肉搏）：本格 + 相邻6格
 * - 守备单位（未进入肉搏）：本格 + 相邻6格
 * - 掠袭单位：本格 + 相邻2格（仅用于补给阻断，不阻止敌军移动）
 * - 野战散乱单位：仅本格（有本格ZOC）
 * - 其他所有状态（行军/休整/运输/肉搏中）：仅本格
 *
 * 叠加规则：敌我 ZOC 可在同一格叠加，该格同时属于双方 ZOC
 *
 * 移动与ZOC：
 * - 单位移动进入敌方ZOC须立刻停止本回合移动（掠袭ZOC例外，不阻止移动）
 *
 * 状态切换限制：只有在敌军 ZOC 外（该格不受任何敌军 ZOC 覆盖）才可切换状态
 *
 * 旗语技能将军：
 * - 指挥距离+2（5格→7格），计算指挥距离的路线可无视ZOC
 * - 将军本队移动不可无视ZOC（本队仍受ZOC停止规则约束）
 */
fun getZocRange(unit: Unit): ZocType = when {
    unit.state == UnitState.FIELD_ORDERLY && !unit.isInMelee -> ZocType.ADJACENT
    unit.state == UnitState.GARRISON && !unit.isInMelee -> ZocType.ADJACENT
    else -> ZocType.SELF_ONLY
}

enum class ZocType { ADJACENT, SELF_ONLY }
```

#### 4.3.5 阵型恢复特殊规则
```kotlin
/**
 * 野战重整阵型可以在以下情况执行（均无视 ZOC 限制）：
 * 1. 在敌方 ZOC 范围内
 * 2. 肉搏战斗中（同格）
 *
 * 肉搏压制例外：若格内存在敌方阵型严整单位，己方散乱单位无法重振（受压制）
 *
 * 堆叠减益：
 *   有效纪律 = max(1, 原纪律 - (同格非野战严整友军数 - 1) - 同格敌军数)
 *   注：只有非野战严整的友军（散乱/脆弱状态友军）才纳入惩罚计算
 *       同格敌军每1支额外 -1 纪律等级
 */
fun calculateEffectiveDisciplineForRecovery(
    originalDiscipline: DisciplineLevel,
    nonOrderlyFriendlyUnitsInHex: Int,  // 同格非野战严整友军数（含本单位）
    enemyUnitsInHex: Int
): DisciplineLevel {
    val penalty = (nonOrderlyFriendlyUnitsInHex - 1) + enemyUnitsInHex
    val effectiveValue = maxOf(1, originalDiscipline.value - penalty)
    return when (effectiveValue) {
        1 -> DisciplineLevel.MOB
        2 -> DisciplineLevel.DISCIPLINED
        else -> DisciplineLevel.STRICT
    }
}

// 示例: 军纪森严部队，同格有2友军+1敌军
// 有效纪律 = 3 - (2-1) - 1 = 1 → 视为乌合之众

fun canRestoreFormation(unit: Unit): Boolean {
    return unit.state == UnitState.FIELD_DISORDERED
    // 成功率 = effectiveDiscipline.recoveryRate × 堆叠修正(0.8/人力)，由骰子判定
}
```

### 4.4 堆叠规则

```kotlin
/**
 * 堆叠上限：5人力（同格所有单位人力点数之和 ≤ 5）
 * 己方进入己方格：可能使原单位散乱（纪律判定，概率 = 1/(3×纪律)）
 * 敌方只能进入散乱或脆弱状态单位所在格（散乱虽属安全状态但仍可被敌方进入）
 * 只有严整单位可主动离开
 * 散乱单位无ZOC；至少一个严整单位则格子有ZOC
 * 多个散乱单位可各自重振，堆叠修正 = ×0.8/同格总人力
 */
fun canStackInHex(unitsInHex: List<Unit>, enteringUnit: Unit): Boolean {
    val totalPersonnel = unitsInHex.sumOf { it.personnel.points } + enteringUnit.personnel.points
    return totalPersonnel <= 5
}
```

### 4.5 训练系统

```kotlin
/**
 * 训练条件：休整状态 + 消耗命令
 * 可自行训练或由教官传授
 * 教官要求：该方面等级 > 受训单位（如体力训练需教官体力等级更高）
 * 有教官时成功率翻倍
 */

enum class TrainingDirection(val baseSuccessRate: Float) {
    STAMINA_MEAGER_TO_ROBUST(0.30f),       // 差强人意→身强力壮: 30%
    STAMINA_ROBUST_TO_STRONG(0.20f),        // 身强力壮→虎背熊腰: 20%
    DISCIPLINE_MOB_TO_DISCIPLINED(0.30f),   // 乌合之众→阵法不乱: 30%
    DISCIPLINE_DISCIPLINED_TO_STRICT(0.20f),// 阵法不乱→军纪森严: 20%
    MORALE_DEPRESSED_TO_STEADY(0.40f),      // 萎靡不振→昂首挺胸: 40%
    MORALE_STEADY_TO_EXCITED(0.25f),        // 昂首挺胸→斗志昂扬: 25%
    MORALE_PANICKED_TO_DEPRESSED(0.50f)      // 惊慌失措→萎靡不振: 50%
}

fun calculateTrainingSuccess(
    direction: TrainingDirection,
    hasInstructor: Boolean
): Float {
    val rate = direction.baseSuccessRate
    return if (hasInstructor) rate * 2f else rate
}
```

### 4.6 威望与叛乱

```kotlin
/**
 * 威望规则：
 * - 每消灭1单位敌军单位提升1点威望
 * - 减少1单位其从属时，有 威望/(威望+5) 概率导致全国士气下降1级
 *
 * 威望触发概率表：
 *   0→0% | 1→16.7% | 2→28.6% | 3→37.5% | 4→44.4% | 5→50% | 10→66.7%
 */

fun calculateMoraleDropChance(prestige: Int): Float {
    if (prestige <= 0) return 0f
    return prestige.toFloat() / (prestige + 5)
}

/**
 * 叛乱机制：
 * - 游戏进行到一定阶段，某将军变为野心家
 * - 野心家可暗藏死士单位，叛乱时突然出现控制重要地点
 * - 叛乱发生后，参谋长不可向野心家及其从属发布命令
 * - 其他将军需回援首都
 *
 * 叛乱触发：
 * - 叛乱将军可随时决定发动叛乱（不限于特定回合）
 * - 叛乱发生后，叛乱将军立即获得补给行动权限
 */

/**
 * 死士系统（野心家将军专属）：
 *
 * 部署规则：
 * - 任意时刻均可部署（包括其他玩家操作期间），其他玩家不可感知
 * - 最多同时存在3支死士单位，均为满员状态
 * - 将军自行选择装备（武器/盾牌），不可选择盔甲
 * - 固定属性：满员(人力2) / 差强人意(体力3) / 乌合之众(纪律1) / 斗志昂扬(士气+1)
 * - 将军选择部署位置后，死士就地隐藏，其他所有人不可见
 * - 部署后位置不可更改
 * - 未叛乱的隐藏死士不受堆叠限制（不占用同格人力配额）
 * - 部署禁区：不得部署于己方首都及其相邻2格范围内
 *
 * 叛乱触发效果：
 * - 叛乱发生后，所有死士立刻在部署位置出现（其他玩家才可见）
 * - 出现后死士纳入正常堆叠限制（若超出5人力上限则需即时处理）
 * - 若死士与敌国单位处于同一格，则死士被立刻消灭
 * - 若该格同时存在本国单位与敌国单位，则本国单位与死士同时被消灭，敌国单位不受影响
 * - 若死士仅与参谋长军队（本国单位）同格（无敌国单位），则立即消灭参谋长军队，死士存活
 * - 死士存活后转为叛乱将军正规部队，属性保持不变，可被正常指挥
 * - 叛乱将军消灭本国参谋长本队或占领本国首都后，自动成为本国参谋长
 *   原参谋长不再参与游戏（进入观战），其余将军不再与叛乱将军敌对
 *
 * 野心家资源点压制特权：
 * - 野心家将军针对本国资源点只需压制1回合即可占领（正常为3回合）
 * - 仅对叛乱前即属己方的资源点生效
 */
data class AssassinUnit(
    val id: String,
    val deployPosition: HexCoord,          // 部署位置（部署后不可更改）
    val carriedEquipment: CarriedEquipment, // 将军自选携带装备（不可选盔甲）
    val equippedGear: EquippedGear,         // 将军自选装备装备（不可选盔甲）
    val isRevealed: Boolean = false         // 是否已暴露（叛乱时全部暴露）
) {
    // 固定属性：满员/差强人意/乌合之众/斗志昂扬
    val fixedPersonnel: PersonnelLevel = PersonnelLevel.FULL     // 满员: 2人力
    val fixedStamina: StaminaLevel = StaminaLevel.MEAGER        // 差强人意: 3
    val fixedDiscipline: DisciplineLevel = DisciplineLevel.MOB   // 乌合之众
    val fixedMorale: MoraleLevel = MoraleLevel.EXCITED           // 斗志昂扬
}

/**
 * 死士部署验证：
 * - 最多3支
 * - 任意时刻均可部署（包括其他玩家操作期间）
 * - 装备不可含盔甲
 * - 部署位置不可与现有死士重叠
 * - 不得部署于己方首都及其相邻2格范围内
 * - 隐藏状态下不受堆叠限制
 */
data class AssassinationDeployment(
    val generalId: String,
    val capitalPosition: HexCoord,        // 己方首都坐标（用于部署禁区判定）
    val positions: List<HexCoord>,
    val carriedEquipments: List<CarriedEquipment>,
    val equippedGears: List<EquippedGear>,
    val deployedCount: Int = 0
) {
    fun canDeploy(targetPos: HexCoord): Boolean {
        // 部署禁区：首都及其相邻2格
        if (capitalPosition.distanceTo(targetPos) <= 2) return false
        return deployedCount < 3
                && carriedEquipments.all { it.armors == 0 }  // 携带装备不可含盔甲
                && equippedGears.all { it.armorCount == 0 }  // 装备装备不可含盔甲
                && positions.distinct().size == positions.size  // 位置不重叠
    }
}

/**
 * 叛乱触发逻辑：
 * 1. 将军宣布叛乱（任意时刻）
 * 2. 所有死士立即暴露（isRevealed = true），纳入正常堆叠限制
 * 3. 检查每支死士所在格：
 *    a. 若死士格内有敌国单位 → 死士被立刻消灭（不发生任何效果）
 *    b. 若死士格内同时有本国单位和敌国单位 → 本国单位与死士同时消灭，敌国单位不受影响
 *    c. 若死士格内只有本国单位（无敌国单位） → 本国单位被立即消灭，死士存活，转为叛乱将军正规军
 *    d. 若死士格内只有己方单位（无本国非己方单位）或为空 → 死士正常转为叛乱将军正规军
 * 4. 叛乱将军获得补给行动权限
 * 5. 野心家将军针对本国资源点压制需求降为1回合
 * 6. 叛乱将军消灭本国参谋长本队或占领本国首都后，自动成为本国参谋长
 *    原参谋长进入观战，其余将军不再与其敌对
 *
 * 注：参谋长军队 = 非野心将军从属的本国所有单位（不只是参谋长本队及其从属）
 */
data class RebellionResult(
    val rebelledGeneralId: String,
    val revealedAssassins: List<AssassinUnit>,
    val eliminatedUnits: List<String>,   // 被消灭的参谋长方单位ID
    val convertedAssassins: List<Unit>   // 转为正规军的死士
)

/**
 * 将军逃生（残部被全灭时）：
 * - 成功：3回合后回归，威望减半
 * - 失败：子嗣继任，威望清零，技能重置
 */
enum class GeneralEscapeResult {
    SUCCESS_RETURN_3_TURNS,  // 3回合后回归，威望减半
    FAILED_SUCCESSION,       // 子嗣继任，威望清零，技能重置
}

/**
 * 残部特殊规则：
 * - 无ZOC（掠袭状态除外，但补给阻断减半）
 * 可进入掠袭状态夺取敌方补给
 * 将军所在单位被击溃 → 变残部
 * 不消耗粮食，但断补仍生效（不能射击、士气下降）
 */
```

### 4.7 胜利条件

```kotlin
/**
 * 各角色胜利条件：
 *
 * 参谋长（本国）：
 *   攻下敌方首都 + 消灭本国叛乱将军
 *
 * 正常将军（本国）：
 *   攻下敌方首都 + 消灭本国叛乱将军
 *
 * 叛乱将军（野心家）：
 *   攻下本国首都 → 成为参谋长 → 指挥剩余将军攻下敌国首都
 */
enum class VictoryCondition(val description: String) {
    NORMAL_VICTORY("攻下敌方首都 + 消灭本国叛乱将军"),
    TRAITOR_STEP_1("攻下本国首都（成为参谋长）"),
    TRAITOR_FINAL("成为参谋长后攻下敌国首都")
}
```

### 4.8 技能获取

```kotlin
/**
 * 技能获取流程：
 * - 每人2个技能
 * - 第1技能：从3个随机技能中选择（玩家自选）
 * - 第2技能：从剩余技能池中随机获得
 * - 同一角色不能获得两个相同技能
 *
 * 将军技能池：旗语/军官团/白骑士/魔王/入阵讨/天才（骑乘技能不属于将军技能池）
 * 参谋长技能池：炼金术师/贤相/马政/鬼才/出身行伍/好为人师
 *
 * 好为人师（参谋长技能）：首都卫戍单位初始拥有所有单位技能（含骑乘、山地作战、盾阵等）
 *
 * 天才（将军技能）——隐身伏击详细规则：
 *   1. 使用时机：指令阶段
 *   2. 效果：指挥一个己方单位按正常规则移动后进入隐身状态（敌方不可见）
 *   3. 隐身移动限制：不可经过或进入敌军ZOC
 *      - 违反则立即现身、停止移动，只能对该敌军发动常规打击（非伏击加成）
 *   4. 伏击触发：敌军进入隐身单位ZOC时
 *      - 隐身单位立即现身
 *      - 敌军立即停止移动且停止一切行动（本回合后续行动全部取消）
 *      - 隐身单位对触发敌军发动一轮打击：相邻→对攻，同一格→肉搏
 *      - 该轮所有打击数值视为+1（攻击力、防御力等均+1）
 *   5. 未触发：若整个回合无敌军进入ZOC，补给阶段自动现身（恢复正常状态）
 *
 * 战场习得（单位技能，自动触发，无需命令）：
 *   山地作战: 山地移动或作战，10%/次
 *   盾阵: 被远射或被对攻，10%/次
 *   大炮也不过如此: 被成功炮击，100%
 *
 * 训练习得技能（有教官时）：
 *   - 休整 + 有骑乘技能单位处于同一格 → 可通过协助训练习得骑乘技能
 *   - 其他通过训练可习得的技能：概率为 1/3
 *   - 注意：技能概率不会因教官翻倍，固定为 1/3
 */

---

## 五、UI/UX 设计要点

### 5.1 界面层级

```
┌─────────────────────────────────────────────────┐
│                  主菜单界面                      │
│  [开始新游戏] [继续游戏] [游戏设置] [规则说明]    │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│                  角色配置界面                    │
│  • 选择阵营                                      │
│  • 生成角色（随机技能）                          │
│  • 地图预览                                      │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│                  游戏主界面                     │
│  ┌─────────┐ ┌───────────────────────────────┐  │
│  │ 部队列表 │ │                               │  │
│  │         │ │        六角格地图              │  │
│  │ 选中详情│ │                               │  │
│  │         │ │                               │  │
│  │ 资源面板│ │                               │  │
│  └─────────┘ └───────────────────────────────┘  │
│  ┌───────────────────────────────────────────┐  │
│  │ 命令栏 / 快捷操作 / 回合结束按钮           │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────────────┐
│              战斗动画界面（弹窗）                │
│  • 攻击方信息                                    │
│  • 防守方信息                                    │
│  • 骰子动画                                      │
│  • 结果展示                                      │
└─────────────────────────────────────────────────┘
```

### 5.2 核心交互

> **UI 交互参考**：手机游戏《远古帝国2》的交互风格，主要借鉴其：
> - 单位点选后弹出环形/扇形快捷命令菜单
> - 地图上直接拖拽路径预览
> - 底部信息面板随选中目标动态变化
> - 手势缩放平移地图的流畅体验

| 交互 | 操作 | 说明 |
|------|------|------|
| 选中单位 | 点击单位 | 显示详细信息面板，弹出快捷命令环 |
| 移动 | 点击目标格 | 显示路径预览（高亮可达格），确认移动 |
| 命令 | 点击命令按钮 | 弹出命令选择菜单（参考扇形菜单） |
| 战斗 | 点击敌方单位 | 弹出战斗确认，展示打击预测 |
| 结束回合 | 点击结束按钮 | 触发回合结算 |
| 缩放地图 | 双指捏合/展开 | 平滑缩放六角格地图 |
| 平移地图 | 单指拖动 | 拖动视野 |

### 5.3 视觉风格建议

- **色调**：复古军事地图风格
- **六角格**：清晰边框，不同地形有明显区分
- **单位标记**：图标+数字，清晰显示状态
- **动画**：简洁的战斗动画和移动轨迹

---

## 六、关键技术实现

### 6.1 六角格地图渲染

```kotlin
@Composable
fun HexGrid(
    map: GameMap,
    selectedUnit: Unit?,
    onTileClick: (HexCoord) -> Unit
) {
    Canvas(modifier = Modifier.fillMaxSize()) {
        map.tiles.forEach { (coord, tile) ->
            val center = hexToPixel(coord)
            val corners = getHexCorners(center, HEX_SIZE)

            // 绘制六角格
            drawHex(corners, tile)

            // 绘制地形
            drawTerrain(corners, tile.terrain)

            // 绘制单位
            tile.unit?.let { unit ->
                drawUnit(corners, unit)
            }

            // 绘制选中高亮
            if (selectedUnit?.position == coord) {
                drawSelectionHighlight(corners)
            }
        }
    }
}

// 六角格像素坐标转换 (Pointy-top)
fun hexToPixel(hex: HexCoord, size: Float): Offset {
    val x = size * (sqrt(3f) * hex.q + sqrt(3f) / 2 * hex.r)
    val y = size * (3f / 2 * hex.r)
    return Offset(x, y)
}
```

### 6.2 回合状态机

```kotlin
sealed class TurnPhase {
    object GeneralCommand : TurnPhase()
    object ChiefOfStaffCommand : TurnPhase()
    object Supply : TurnPhase()
    object Settlement : TurnPhase()
}

class TurnManager {
    private val _currentPhase = MutableStateFlow<TurnPhase>(TurnPhase.GeneralCommand)
    val currentPhase: StateFlow<TurnPhase> = _currentPhase

    private val _currentCommander = MutableStateFlow<Character?>(null)
    val currentCommander: StateFlow<Character?> = _currentCommander

    private val _executedCommands = mutableMapOf<String, Set<String>>()

    fun executeCommand(command: Command): Result<Unit> {
        // 检查命令是否有效
        // 执行命令
        // 更新状态
    }

    fun nextPhase() {
        _currentPhase.value = when (_currentPhase.value) {
            TurnPhase.GeneralCommand -> TurnPhase.ChiefOfStaffCommand
            TurnPhase.ChiefOfStaffCommand -> TurnPhase.Supply
            TurnPhase.Supply -> TurnPhase.Settlement
            TurnPhase.Settlement -> TurnPhase.GeneralCommand
        }
    }

    fun endTurn() {
        // 回合结算
        // 重置命令状态
        // 进入下一回合
    }
}
```

### 6.3 存档系统

```kotlin
@Entity(tableName = "game_saves")
data class GameSaveEntity(
    @PrimaryKey val id: String,
    val turnNumber: Int,
    val currentPhase: String,
    val saveData: String,  // JSON 序列化的完整游戏状态
    val timestamp: Long
)

class GameRepository {
    suspend fun saveGame(gameState: GameState): Result<Unit>
    suspend fun loadGame(saveId: String): Result<GameState>
    suspend fun getAllSaves(): List<GameSaveEntity>
    suspend fun deleteSave(saveId: String): Result<Unit>
}
```

### 6.4 移动力计算

```kotlin
/**
 * 单位移动力公式：移动力 = 体力 - 装备重量
 *
 * 体力来源：
 *   - 基础体力：StaminaLevel.value（3/4/5）
 *   - 牲畜体力：牛或马各提供 +2 体力（无骑乘技能时不转化为移动力）
 *
 * 无骑乘技能：
 *   - 装备重量优先消耗牲畜体力部分
 *   - 牲畜体力不纳入移动力计算
 *   - 移动力 = 基础体力 - max(0, 装备重量 - 牲畜体力)
 *
 * 有骑乘技能且有马（骑兵状态）：
 *   - 移动力 = (基础体力 + 马匹体力) - 装备重量
 *
 * 山地移动（山地作战技能）：
 *   - 进入山地格：消耗 2（与无技能相同）
 *   - 在已处于山地格之间移动：消耗 1（技能减益）
 */
fun calculateMovement(unit: Unit): Int {
    val baseStamina = unit.stamina.value
    val livestockStamina = unit.livestockList.sumOf { it.staminaBonus }
    val hasRiding = unit.skills.any { it.type == SkillType.RIDING }
    val hasHorse = unit.livestockList.any { it.type == LivestockType.HORSE }

    return if (hasRiding && hasHorse && unit.isCavalry) {
        (baseStamina + livestockStamina) - unit.carriedEquipment.getTotalWeight()
    } else {
        val weightAfterLivestock = maxOf(0, unit.carriedEquipment.getTotalWeight() - livestockStamina)
        baseStamina - weightAfterLivestock
    }
}

fun getMoveCost(unit: Unit, fromTerrain: TerrainType, toTerrain: TerrainType): Int {
    val hasMountainCombat = unit.skills.any { it.type == SkillType.MOUNTAIN_COMBAT }
    return when {
        toTerrain == TerrainType.MOUNTAIN && fromTerrain == TerrainType.MOUNTAIN && hasMountainCombat -> 1
        toTerrain == TerrainType.MOUNTAIN -> 2
        toTerrain == TerrainType.RIVER -> 2
        else -> 1
    }
}
```

### 6.5 工事系统

```kotlin
/**
 * 天生工事（资源点自带，全朝向）：
 *   村庄: 1级 | 城镇: 2级 | 城市: 3级
 *   天生工事可被炮击按朝向摧毁，摧毁后永久消失（可人工重建）
 *
 * 人工建设工事（休整状态单位，一个朝向一个朝向地建）：
 *   1级 = 8进度 | 2级 = 20进度 | 3级 = 40-50进度
 *   建设进度 = Σ(人力 × 剩余体力) + Σ(牲畜体力)
 *   残部不参与建设，至少需要1回合
 *
 * 工事防御掷骰（只有守备状态才生效）：
 *   1级 = 2颗骰子取较小值
 *   2级 = 3颗骰子取较小值
 *   3级 = 4颗骰子取较小值
 *
 * 工事生效规则：
 * - 只有守备状态工事才能生效
 * - 只有攻击来向的工事朝向生效
 * - 只有阵型打击成功才可以冲进工事肉搏
 * - 占领方需转为守备状态才能激活工事
 */
data class Fortification(
    val directionLevels: Map<Int, Int> = emptyMap(),  // key: 0-5方向, value: 剩余级别
    val isNatural: Boolean = false  // true=建筑天然工事, false=人工工事
)

// 工事建设进度需求
fun getFortBuildProgressRequired(level: Int): Int = when (level) {
    1 -> 8
    2 -> 20
    3 -> 45  // 40-50取中间值
    else -> 0
}

// 建设进度贡献计算
fun calculateBuildProgress(units: List<Unit>): Int {
    return units.filter { it.personnel != PersonnelLevel.REMNANT }.sumOf { unit ->
        unit.personnel.points * unit.calculateMovement() + unit.livestockList.sumOf { it.staminaBonus }
    }
}

// 工事防御掷骰
fun rollFortificationDefense(fortLevel: Int): Int {
    if (fortLevel <= 0) return 0
    val diceCount = fortLevel + 1  // 1级=2骰, 2级=3骰, 3级=4骰
    return (1..diceCount).map { (1..6).random() }.minOrNull() ?: 0
}

// 炮击摧毁工事
fun applyArtilleryToFortification(
    fort: Fortification,
    direction: Int
): Fortification {
    val current = fort.directionLevels[direction] ?: 0
    if (current <= 0) return fort
    return fort.copy(
        directionLevels = fort.directionLevels + (direction to (current - 1))
    )
}
```

### 6.6 补给阶段：牲畜与装备输送

```kotlin
/**
 * 补给阶段可输送的物资：
 *
 * 1. 储备装备（携带装备）输送：
 *    - 使用补给完粮食后仍有剩余容量的运输通道
 *    - 可从资源点储备→目标单位（加入目标单位的携带装备）
 *    - 可从单位→单位（携带装备间转移）
 *    - 消耗运输通道流量
 *
 * 2. 牲畜（牛/马）输送：
 *    - 不消耗运输通道流量（与粮食/装备独立）
 *    - 补给阶段输送完毕，下一命令阶段生效
 *
 * 断补第2回合处理（优先消耗牲畜）：
 *   if (unit.livestockList.isNotEmpty()) {
 *       消耗所有牲畜（livestockList = emptyList()），本回合不降士气
 *   } else {
 *       直接降低士气
 *   }
 */
data class SupplyPackage(
    val food: Int = 0,
    val equipment: Int = 0,
    val livestock: Livestock? = null  // 可输送牛或马
)
```

---

## 七、开发里程碑

### Phase 1: 核心框架（2-3周）
- [ ] 项目初始化与架构搭建（含联机/单机模式切换框架）
- [ ] 六角格地图渲染基础
- [ ] 基础数据模型实现
- [ ] 地图缩放与平移交互（参考远古帝国2手势）

### Phase 2: 回合系统（2-3周）
- [ ] 回合流程状态机（将军/参谋长/补给/结算四阶段）
- [ ] 补给阶段详细流程（产出→补给判定→组建→参谋长调装备→将军调装备→牲畜输送→生产指派→压制→断补）
- [ ] 单位移动系统（含移动力公式、山地技能）
- [ ] 状态切换系统（安全/脆弱、装备装备状态限制、守备回合限制）
- [ ] 基础命令系统（本队免费 + 配额限制 + 装备切换）
- [ ] 地图点击交互

### Phase 3: 战斗系统（2-3周）
- [ ] 战斗计算引擎（三项打击独立结算 + 防御力基础值）
- [ ] 四种战斗类型（炮击/远射/对攻/肉搏含散乱减益）
- [ ] 战斗动画界面
- [ ] ZOC 系统（野战/守备/掠袭不同范围）、状态切换限制
- [ ] 溃散判定

### Phase 4: 资源与补给（3周）
- [ ] 资源点产出系统（村庄/城镇/城市/马场产出周期）
- [ ] 补给线流量机制（输送能力、传递范围、瓶颈、ZOC阻断）
- [ ] 断补效果实现（含牲畜优先消耗、最远优先断补）
- [ ] 补给阶段储备装备/粮食/牲畜输送（储备装备使用补完粮食后剩余容量）
- [ ] 装备生产（持续生产机制）
- [ ] 据点压制（3回合归属变更）
- [ ] 新兵团组建（当回合锁定）
- [ ] 补员系统（部分/分批吸收、属性副作用）

### Phase 5: 堆叠与散乱（1-2周）
- [ ] 5人力上限堆叠
- [ ] 多单位堆叠进入散乱判定
- [ ] 离开堆叠格限制（严整才可离开）
- [ ] 野战重整阵型堆叠减益（友军+敌军，×0.8/人力修正）

### Phase 6: 角色与技能（2周）
- [ ] 角色系统（含野心家将军、本队机制、技能获取流程）
- [ ] 将军技能（白骑士/魔王"视为"效果、入阵讨、天才伏击）
- [ ] 参谋长技能（炼金术师/贤相/马政/鬼才等）
- [ ] 骑乘技能与骑兵系统
- [ ] 战场习得技能（山地作战/盾阵/大炮也不过如此）

### Phase 7: 特殊机制（1-2周）
- [ ] 威望系统（消灭敌军+1、减少从属触发概率）
- [ ] 叛乱机制（死士部署/隐藏/暴露、参谋长禁令、回援首都）
- [ ] 将军逃生判定
- [ ] 掠袭机制（2格ZOC、阻断夺取50%）
- [ ] 训练系统（7方向成功率、教官翻倍）
- [ ] 工事系统（建设进度、防御掷骰、朝向摧毁）

### Phase 8: 联机支持（2-3周）
- [ ] 房间创建/加入（4-8人）
- [ ] WebSocket 实时消息同步
- [ ] 断线重连与状态快照

### Phase 9: 完善与测试（2周）
- [ ] AI 逻辑（单机版 AI 对手）
- [ ] 存档/读档
- [ ] UI 美化
- [ ] Bug 修复与平衡调整

---

## 八、附录：数值速查表

### 地形属性
| 地形 | 移动消耗 | 视野 | 防御加成 | 特殊效果 |
|------|---------|------|---------|---------|
| 平地 | 1 | 正常 | 无 | 基准地形 |
| 山地 | 2（进入）/ 1（山地间移动，需山地作战技能） | -1格 | +1 | 进入散乱（山地作战免疫）；战斗2骰选优；骑兵增益取消 |
| 河流 | 2（需渡口） | 正常 | 无 | 跨河攻击-2 |
| 建筑群 | 1 | 正常 | 工事等级（可被炮击按朝向摧毁） | 村庄1级/城镇2级/城市3级；资源点不被摧毁 |
| 不可进入 | — | 阻断 | — | 高山/湖泊无法通过 |

### 体力等级
| 等级 | 数值 |
|------|------|
| 差强人意 | 3 |
| 身强力壮 | 4 |
| 虎背熊腰 | 5 |

### 士气等级
| 等级 | 打击修正 | 防御修正 |
|------|---------|---------|
| 斗志昂扬 | +1 | 0 |
| 昂首挺胸 | 0 | 0 |
| 萎靡不振 | -1 | 0 |
| 惊慌失措 | -1 | -1 |

### 纪律等级
| 等级 | 等级值 | 恢复成功率 | 进入建筑群/严整格散乱概率 |
|------|-------|-----------|------------------------|
| 乌合之众 | 1 | 50% | 50% |
| 阵法不乱 | 2 | 66.7% | 66.7% |
| 军纪森严 | 3 | 75% | 75% |

> **注**：堆叠重整时，纪律等级惩罚 = (同格非野战严整友军数-1) + 同格敌军数（最低为1）

### 牲畜系统
| 牲畜 | 体力加成 | 无骑乘技能 | 有骑乘技能（马） |
|------|---------|-----------|----------------|
| 牛 | +2 | 不增加移动力，装备优先消耗 | 同左（牛不能骑乘） |
| 马 | +2 | 不增加移动力，装备优先消耗 | 可切换骑兵，体力转为移动力 |

> **牲畜上限**：一个单位最多同时拥有1牛和1马
> **牲畜输送**：补给阶段可通过补给线将牲畜（牛/马）输送至前线单位，与粮食/装备同阶段装备
> **牲畜逃散**：当单位所在格有敌军且单位非野战严整状态时，牲畜逃散并带走携带装备（储备）中的全部武器和盔甲。有骑乘技能的单位马不逃散（但牛仍逃散）。逃散后储备装备不可恢复。

### 武器效果
| 武器 | 对攻阵型 | 对攻减员 | 对攻士气 | 肉搏阵型 | 肉搏减员 | 肉搏士气 |
|------|---------|---------|---------|---------|---------|---------|
| 长矛 | 2 | 1 | 1 | 1 | 1 | 0 |
| 剑 | 1 | 1 | 1 | 0 | 3 | 1 |
| 弓 | 1 | 1 | 1 | 1 | 1 | 1 |

> **注**：骑兵对攻阵型打击额外+1，**山地地形除外**（山地骑兵无此增益）；面对军纪森严单位时无效

### 炮击成功率
| 距离 | 普通 | 首次 |
|------|------|------|
| 1格 | 50% | 66.7% |
| 2格 | 33.3% | 50% |
| 3格 | 25% | 40% |
| 4格 | 20% | 33.3% |
| 5格 | 16.7% | 28.6% |

### 装备负重与成本
| 装备 | 负重 | 生产成本 | 防御加成(装备装备) | 叠加 |
|------|------|---------|-------------------|------|
| 长矛 | 1 | 1 | — | — |
| 剑 | 1 | 1 | — | — |
| 弓 | 1 | 1 | — | — |
| 盾牌 | 1 | 1 | 对攻/肉搏+1，不叠加 | 不叠加 |
| 盔甲 | 1 | 2 | +1/件（远射/对攻/肉搏均加） | 无上限 |
| 火炮 | 4 | 2（需炼金术师） | — | — |

> **携带装备 vs 装备装备**：
> - 携带装备（储备）= 库存，无数量/重量上限，体力仅影响移动力
> - 装备装备（实际生效）= 当前穿戴，受状态限制：
>   安全状态（严整/散乱/守备）：总重量 ≤ 体力
>   脆弱状态（行军/休整/运输/掠袭）：总重量 ≤ 1
> - 切换装备装备在命令阶段消耗命令；流通储备装备在补给阶段
> - 多武器装备时打击能力取各武器最大值；无武器不能发起打击；有武器即使能力为0也可掷骰

### 工事建设进度与防御
| 工事等级 | 建设进度 | 防御掷骰 |
|---------|---------|---------|
| 1级 | 8 | 2颗骰子取较小值 |
| 2级 | 20 | 3颗骰子取较小值 |
| 3级 | 40-50 | 4颗骰子取较小值 |

> 建设进度 = Σ(人力 × 剩余体力) + Σ(牲畜体力)；仅守备状态生效

### 训练成功率
| 提升方向 | 成功率 | 有教官 |
|---------|-------|-------|
| 差强人意→身强力壮 | 30% | 60% |
| 身强力壮→虎背熊腰 | 20% | 40% |
| 乌合之众→阵法不乱 | 30% | 60% |
| 阵法不乱→军纪森严 | 20% | 40% |
| 萎靡不振→昂首挺胸 | 40% | 80% |
| 昂首挺胸→斗志昂扬 | 25% | 50% |
| 惊慌失措→萎靡不振 | 50% | 100% |

### 兵力对战斗的影响
| 当前兵力 | 打击骰数 | 取值方式 | 防御力修正 |
|---------|---------|---------|-----------|
| 满编(2人力) | 1颗 | 直接取值 | 0 |
| 缺员(1人力) | 2颗 | 取较小值 | -1 |
| 残部(0人力) | 3颗 | 取较小值 | -2 |

> 防御力 = 基础防御力(5或4) + 装备防御加成(装备装备) + 兵力修正 + 士气修正 - 肉搏散乱减益

### 兵力恢复装备丢失
| 恢复方向 | 丢失概率 | 丢失方式 | 不可丢失 |
|---------|---------|---------|---------|
| 残部→缺员 | 100% | 随机1件(非火炮) | 火炮 |
| 残部→满编 | 100% | 随机1件(非火炮) | 火炮 |
| 缺员→满编 | 30% | 随机1件(非火炮) | 火炮 |

> 丢失装备同时从携带装备和装备装备中移除

### 战场习得技能（属于单位技能，由单位自身通过行动习得）
| 技能 | 获得条件 | 概率 |
|------|---------|------|
| 山地作战 | 山地移动或作战 | 10%/次 |
| 盾阵 | 被远射或被对攻 | 10%/次 |
| 大炮也不过如此 | 被成功炮击 | 100% |

### 威望触发概率
| 威望 | 减少从属时全国士气下降概率 |
|------|------------------------|
| 0 | 0% |
| 1 | 16.7% |
| 2 | 28.6% |
| 3 | 37.5% |
| 4 | 44.4% |
| 5 | 50% |
| 10 | 66.7% |

### 死士系统（野心家将军）
| 项目 | 规则 |
|------|------|
| 部署时机 | **任意时刻**（包括其他玩家操作期间，其他玩家不可感知） |
| 数量上限 | 最多3支 |
| 兵员状态 | 满员(2人力) |
| 固定属性 | 满员(人力2) / 差强人意(体力3) / 乌合之众(纪律1) / 斗志昂扬(士气+1) |
| 装备限制 | 将军自选武器/盾牌，不可选盔甲 |
| 部署禁区 | **不得部署于己方首都及其相邻2格范围内** |
| 部署规则 | 选择位置后就地隐藏，其他玩家不可见 |
| 堆叠限制 | 隐藏状态下不受堆叠限制；叛乱暴露后纳入正常堆叠 |
| 位置修改 | 部署后不可更改 |
| 叛乱触发 | 野心家将军随时可发动叛乱 |
| 出现时机 | 叛乱发生后所有死士立即暴露出现 |
| 与敌国单位同格 | 死士被立刻消灭 |
| 与本国+敌国同格 | 本国单位与死士同时消灭，敌国单位不受影响 |
| 仅与本国单位同格 | 立即消灭本国单位，死士存活 |
| 战后状态 | 死士存活，转为叛乱将军正规部队继续战斗 |
| 叛乱后补给 | 叛乱将军叛乱后可执行补给行动 |
| 资源点压制 | 野心家将军针对本国资源点只需压制1回合即可占领 |
| 成为参谋长 | 消灭本国参谋长本队或占领本国首都后，自动成为参谋长 |

---

*文档版本：1.7*
*更新内容（v1.7）：*
- *骑乘技能移出将军技能池，归为单位技能，只能由特定兵源地天生拥有或同格休整单位协助训练获得*
- *守备3回合无战斗后的强制转换机制修正：补给阶段降士气为萎靡不振后立刻转为休整*
- *进入守备状态新增前置条件：士气须为昂首挺胸及以上*
- *删除"将军和参谋长均可下令退出运输状态"，改为统一说明：退出状态=进入新目标状态*
- *肉搏状态规则：阵型严整一方可主动离开肉搏格；散乱一方受压制无法重振*
- *盾牌防御说明修正：只在对攻和肉搏时+1防御，远射不加成；盔甲对所有战斗类型生效*
- *兵力恢复（补员）规则全面重写：双方同格休整状态进行吸收；恢复1级50%/恢复2级80%概率素质和技能变为被吸收单位属性；装备丢失改为按携带装备差值判定，连续判定直至失败*
- *因补员失去技能的单位可自行训练恢复，无需协助*
- *新增城市包围机制：所有产粮路线被封锁时停产，最近可达产粮资源点每回合消耗1粮食*
- *处于压制状态的资源点不能组建军队*
- *ZOC规则修正：散乱单位有本格ZOC而非无ZOC；掠袭ZOC只用于补给阻断，不阻止敌军移动*
- *旗语技能明确：指挥距离计算无视ZOC，但将军本队移动本身仍受ZOC停止规则约束*
- *进入建筑群散乱规则：只有野战状态触发，概率改为纪律/(纪律+1)*
- *进入含有单位格散乱：只有野战严整进入另一野战严整格触发，概率同上*
- *打击结算新增：阵型打击成功对方非严整→降纪律1级；工事防御与兵力下降打击叠加*
- *打击目标选择规则：优先打非严整单位，防守方选择受击时从严整单位中选*
- *对攻冲锋条件细化：阵型打击成功且格内无其他严整单位才可进入；工事守备需对攻打散才可进*
- *堆叠离开规则修正：肉搏状态只有严整方可离开；全友军时需判定概率*
- *阵型恢复堆叠减益公式修正：只有非野战严整友军计入惩罚*
- *训练技能习得概率：固定1/3，不受教官翻倍影响*
- *死士部署禁区：不得部署于己方首都及相邻2格内*
- *死士叛乱效果细化：与敌国同格则死士消灭；本国+敌国同格则两者皆消灭；仅本国则消灭本国单位*
- *叛乱将军晋升参谋长条件：消灭本国参谋长本队或占领本国首都*
- *补给阶段顺序修正：产出→粮食补给→组建→参谋长调装备（交替）→将军调装备（交替）→牲畜输送→生产指派→压制→断补*
- *明确参谋长军队：指非野心将军从属的本国所有单位*
- *战场习得技能明确为「单位技能」，由单位自身通过行动习得，非将军技能*
- *多格城市压制判定：所有格均只有己方单位（无任何敌方单位）才算压制；原方进入任意一格取消压制*
- *将军技能「天才」隐身伏击完整规则：指令阶段移动后隐身；不可进敌军ZOC（否则现身+常规打击）；敌军进ZOC则现身+敌军停止一切行动+伏击打击（数值+1）；未触发则补给阶段现身*
- *参谋长技能「好为人师」：首都卫戍单位初始拥有所有单位技能*
*更新时间：2026-04-18*

*更新内容（v1.5）：*
- *装备系统重构为「携带装备」(CarriedEquipment) 和「装备装备」(EquippedGear) 双层模型*
- *战斗计算仅使用装备装备，携带但未装备的不参与打击/防御*
- *打击发起规则：有武器即可发起打击，即使打击能力算出来为0也可掷骰；无武器不能打击*
- *兵力影响打击骰：满编1颗/缺员2颗取小/残部3颗取小*
- *兵力影响防御力：防御力=基础防御力+装备加成+兵力修正（满编0/缺员-1/残部-2）*
- *兵力恢复装备丢失：残部→缺员或满编必丢1件(除火炮)；缺员→满编30%丢1件(除火炮)；均为随机*
- *新增 calculateFullDefense()、getAttackDiceCount()、rollAttackDice() 等战斗计算函数*
- *新增 EquipmentLossResult、checkEquipmentLossOnRecovery() 等装备丢失判定*

*更新内容（v1.4）：*
- *牲畜系统改为列表结构（livestockList），支持一个单位同时拥有1牛1马*
- *牲畜逃散规则：有敌军同格且非野战严整时，牲畜逃散并带走全部武器和盔甲；骑乘技能马不逃散*
- *叛乱死士系统：任意时刻部署（最多3支）、满员状态、固定属性、不可选盔甲*
- *死士隐藏机制：部署后就地隐藏不可见、不受堆叠限制、位置不可更改*
- *叛乱触发：野心家将军随时发动叛乱，死士立即暴露出现并纳入堆叠*
- *死士同格消灭参谋长军队后存活，转为叛乱将军正规部队继续战斗*
- *叛乱后将军可执行补给行动*
- *野心家将军针对本国资源点压制1回合即可占领*
- *新增命令类型：DeployAssassin（部署死士）、StartRebellion（发动叛乱）*

*更新内容（v1.3）：*
- *状态系统完善：安全/脆弱分级、战斗装备上限、可被敌方进入条件、守备3回合限制*
- *状态转换完整规则表（含成功率、条件、ZOC限制例外）*
- *装备系统重写：负重表、生产成本、防御叠加（盾牌不叠加/盔甲无上限）、多武器取最大值*
- *战斗系统补充：防御力基础值（严整/守备=5，其余=4）、远射移动力双降机制、肉搏散乱减益*
- *溃散规则：惊慌失措+非严整→残部*
- *白骑士/魔王效果改为"视为"等级（含战斗判定和溃散判定）*
- *补给系统重写：输送能力计算、传递范围=ceil(移动力/2)、流量瓶颈、ZOC阻断*
- *掠袭机制：相邻2格ZOC、阻断敌方补给夺取50%*
- *补给阶段流程细化：资源产出→补给判定→组建新兵→装备输送→牲畜输送→生产指派→压制判定→断补更新*
- *装备生产：参谋长指定资源点生产，部分装备需2回合*
- *新兵团组建：补给阶段组建，当回合锁定不可移动/吸收*
- *补员规则：部分吸收、分批吸收、属性副作用概率、技能丧失*
- *资源经济：产出周期、储存减半归占领方、据点压制（3回合归属变更）*
- *工事系统：建设进度公式、防御掷骰规则、朝向生效规则、天生工事可被炮击摧毁*
- *堆叠规则：5人力上限、堆叠重振修正×0.8/人力*
- *训练系统：7个方向成功率表、教官翻倍*
- *威望系统：消灭敌军+1、减少从属触发概率*
- *叛乱机制：死士单位、参谋长禁令、回援首都*
- *将军逃生：成功3回合回归威望减半/失败子嗣继任*
- *胜利条件：参谋长/将军/叛乱将军各自条件*
- *技能获取：第1技能3选1、第2技能随机*
- *残部特殊规则：无ZOC/不消耗粮食/断补仍生效*
- *森林地形确认移除*
- *山地作战技能：进入不散乱+纪律视为+1+山地间消耗1（三条并存）*
- *掠袭ZOC范围改为相邻2格*
*更新时间：2026-04-17*
