# 阶段2：回合系统

## 开发目标

实现回合流程状态机和基础命令系统，让玩家能够选中单位、下达移动命令、切换单位状态。

## 预期成果

一个可进行回合制游戏的最小可运行版本：
- 回合四阶段流程（将军命令 → 参谋长命令 → 补给阶段 → 回合结算）
- 单位选中、路径预览、移动命令
- 单位状态切换
- 基础命令配额系统

## 开发内容

### 2.1 回合流程状态机

**模块：阶段枚举**
- 定义 `TurnPhase` 封闭类
- 实现 `GeneralCommand`（将军命令阶段）
- 实现 `ChiefOfStaffCommand`（参谋长命令阶段）
- 实现 `Supply`（补给阶段）
- 实现 `Settlement`（回合结算阶段）

**模块：回合管理器**
- 实现 `TurnManager` 类
- 管理当前阶段和当前指挥官
- 实现阶段切换 `nextPhase()`
- 实现回合结束 `endTurn()`
- 管理命令执行历史

**模块：回合UI**
- 显示当前回合数
- 显示当前阶段
- 显示当前操作的指挥官
- 阶段切换动画

### 2.2 单位选择与信息显示

**模块：单位选中**
- 点击地图上的单位进行选中
- 显示选中单位信息面板
- 高亮显示选中单位
- 显示单位快捷操作环

**模块：单位信息面板**
- 显示单位名称和阵营
- 显示单位状态（人力、体力、士气、纪律）
- 显示当前位置坐标
- 显示装备信息（携带/装备）
- 显示移动力

### 2.3 移动命令系统

**模块：路径计算**
- 实现 A* 或简单六角格路径算法
- 计算从当前位置到目标位置的最优路径
- 考虑地形移动消耗
- 计算剩余移动力

**模块：路径预览**
- 在地图上高亮显示可达格
- 显示移动路径预览线
- 显示路径消耗
- 显示到达后的剩余移动力

**模块：移动执行**
- 点击目标格确认移动
- 更新单位位置
- 扣除移动力
- 播放移动动画

### 2.4 状态切换系统

**模块：状态定义**
- 野战严整 `FIELD_ORDERLY`（仅将军可下令）
- 野战散乱 `FIELD_DISORDERED`
- 守备 `GARRISON`（需士气≥昂首挺胸）
- 行军 `MARCH`
- 休整 `REST`
- 运输 `TRANSPORT`
- 掠袭 `RAID`

**模块：状态限制**
- ZOC 外才能切换状态
- 守备状态最多3回合
- 脆弱状态装备限制（≤1）

**模块：状态切换UI**
- 状态切换命令按钮
- 状态预览（切换后效果）
- 切换成功/失败提示

### 2.5 命令配额系统

**模块：命令限制**
- 普通将军：6支从属单位命令
- 本队命令不计入配额
- 命令距离限制（5格基础）

**模块：命令执行记录**
- 记录已执行的命令
- 显示剩余命令配额
- 命令执行顺序追踪

### 2.6 补给阶段基础

**模块：阶段流程**
- 资源产出结算
- 粮食补给判定
- 组建新兵团
- 装备调整

**模块：补给UI**
- 补给阶段进度显示
- 资源产出提示
- 组建新兵界面

## 可运行标准

1. 游戏开始时显示回合1将军命令阶段
2. 点击己方单位可以选中
3. 选中单位后显示可达范围
4. 点击目标格可以移动单位
5. 可以切换单位状态
6. 回合能正常流转到下一阶段
7. 将军命令阶段 → 参谋长命令阶段 → 补给阶段 → 回合结算 → 新回合

## 技术要点

- **状态机模式**：使用 sealed class 实现回合阶段状态机
- **命令模式**：使用 Command 模式封装操作
- **路径算法**：六角格 A* 寻路，考虑地形消耗
- **UI 状态**：Compose State + StateFlow 管理选中/移动状态

## 依赖项（延续阶段1）

```kotlin
// 阶段1的所有依赖

// 额外依赖
implementation 'androidx.compose.foundation:foundation' // Canvas绘制增强
```

## 里程碑检查清单

- [ ] 回合四阶段正确流转
- [ ] 可以选中地图上的单位
- [ ] 显示单位详细信息
- [ ] 路径预览正确计算
- [ ] 移动命令正确执行
- [ ] 状态切换正确执行
- [ ] 命令配额正确限制
- [ ] 补给阶段界面可用
- [ ] 回合结算正确处理
