# 阶段1：核心框架

## 开发目标

构建项目基础架构，实现六角格地图的渲染与交互，为后续游戏功能开发提供技术框架。

## 预期成果

一个可运行的 Android 应用骨架，包含：
- 六角格地图的渲染与显示
- 基础数据模型的定义
- 地图的缩放、平移交互
- 项目架构（MVVM + Clean Architecture）

## 开发内容

### 1.1 项目初始化

**模块：项目结构**
- 创建 Kotlin + Jetpack Compose 项目
- 配置 Gradle 依赖（Compose BOM、Material3、Lifecycle、Hilt）
- 设置项目包结构（UI/Domain/Data layers）
- 配置代码规范（ktlint、detekt）

**模块：架构框架**
- 实现基础 Clean Architecture 分层
- 集成 Hilt 依赖注入
- 配置 Navigation Compose
- 设置 Coroutines + Flow 异步框架

### 1.2 六角格地图渲染

**模块：坐标系统**
- 实现六角格坐标类 `HexCoord`
- 实现邻居计算方法 `getNeighbors()`
- 实现六角格距离计算 `distanceTo()`
- 实现 Cube 坐标转换

**模块：地形系统**
- 定义地形枚举 `TerrainType`（平原、山地、河流、建筑群、不可通行）
- 实现地形属性（移动消耗、视野、防御加成）
- 定义六角格瓦片类 `HexTile`

**模块：地图渲染**
- 使用 Canvas 绘制六角格
- 实现 Pointy-top 六角格布局
- 绘制不同地形的视觉样式
- 实现六角格边框和填充

**模块：地图交互**
- 实现地图缩放（双指捏合）
- 实现地图平移（单指拖动）
- 实现六角格点击事件
- 实现点击高亮效果

### 1.3 基础数据模型

**模块：枚举定义**
- 阵营枚举 `Faction`
- 兵种等级 `PersonnelLevel`
- 体力等级 `StaminaLevel`
- 士气等级 `MoraleLevel`
- 纪律等级 `DisciplineLevel`

**模块：单位基础属性**
- 定义 `Unit` 数据类（位置、阵营、人力、体力）
- 定义单位状态 `UnitState`（野战严整、野战散乱、行军、休整、守备、运输、掠袭）
- 定义单位安全状态 `UnitSafety`

**模块：装备基础模型**
- 定义武器类型 `WeaponType`（长矛、剑、弓）
- 定义防御装备类型 `DefensiveGearType`（盾牌、盔甲）
- 定义火炮 `ArtilleryGear`
- 定义携带装备 `CarriedEquipment`
- 定义装备装备 `EquippedGear`

### 1.4 地图数据与显示

**模块：地图数据**
- 创建示例地图数据（固定大小 10x10）
- 定义资源点类型 `ResourceType`（村庄、城镇、城市、马场）
- 定义首都位置

**模块：UI 界面**
- 创建主菜单界面
- 创建游戏主界面布局
- 集成地图组件
- 显示单位标记

## 可运行标准

1. 应用启动后显示主菜单界面
2. 点击"开始游戏"可进入游戏主界面
3. 游戏主界面显示六角格地图
4. 地图支持缩放和平移操作
5. 点击六角格可显示该格信息
6. 项目架构清晰，可进行后续开发

## 技术要点

- **六角格算法**：采用 Pointy-top 布局，使用 Cube 坐标系统
- **Canvas 性能**：使用 `remember` 和 `derivedStateOf` 优化重绘
- **手势处理**：使用 `PointerInputScope` 实现自定义手势
- **状态管理**：使用 StateFlow 管理地图和单位状态

## 依赖项

```groovy
// Compose BOM
implementation platform('androidx.compose:compose-bom:2024.02.00')

// Core Compose
implementation 'androidx.compose.ui:ui'
implementation 'androidx.compose.ui:ui-graphics'
implementation 'androidx.compose.ui:ui-tooling-preview'

// Material3
implementation 'androidx.compose.material3:material3'

// Lifecycle
implementation 'androidx.lifecycle:lifecycle-runtime-ktx'
implementation 'androidx.lifecycle:lifecycle-viewmodel-compose'

// Navigation
implementation 'androidx.navigation:navigation-compose'

// Hilt
implementation 'com.google.dagger:hilt-android'
kapt 'com.google.dagger:hilt-compiler'

// Coroutines
implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android'
```

## 里程碑检查清单

- [ ] 项目成功编译运行
- [ ] 主菜单界面正常显示
- [ ] 六角格地图正确渲染
- [ ] 地图缩放功能正常
- [ ] 地图平移功能正常
- [ ] 点击六角格有响应
- [ ] 单元测试通过率 > 80%
