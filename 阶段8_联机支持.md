# 阶段8：联机支持

## 开发目标

实现多人联机对战功能，支持4-8人房间对战。

## 预期成果

一个可联机对战的游戏版本：
- 房间创建与加入
- WebSocket 实时同步
- 断线重连
- 命令同步验证

## 开发内容

### 8.1 房间系统

**模块：房间创建**
- 选择地图
- 设置玩家数量（4-8人）
- 房主设置
- 房间号生成

**模块：房间加入**
- 输入房间号加入
- 显示房间内玩家
- 等待/开始游戏

**模块：玩家配置**
- 选择阵营
- 分配将军/参谋长
- 技能分配

### 8.2 网络同步

**模块：WebSocket**
- Ktor WebSocket 实现
- 房间状态同步
- 命令发送与接收

**模块：消息类型**
- 房间状态更新
- 命令执行
- 回合阶段切换
- 聊天消息

### 8.3 命令同步

**模块：命令验证**
- 房主验证所有命令
- 客户端只负责渲染
- 验证失败通知

**模块：状态同步**
- 完整游戏状态快照
- 增量更新
- 同步频率控制

### 8.4 断线重连

**模块：检测机制**
- 心跳检测
- 断线判定

**模块：重连流程**
- 重连请求
- 发送完整状态快照
- 恢复游戏

### 8.5 UI界面

**模块：房间大厅**
- 创建房间
- 加入房间
- 房间列表

**模块：联机游戏**
- 玩家列表
- 连接状态
- 断线提示

## 可运行标准

1. 可以创建房间
2. 可以加入房间
3. 房间状态正确同步
4. 命令正确同步执行
5. 断线可重连

## 技术要点

- **Ktor**：WebSocket 服务端/客户端
- **状态同步**：完整快照 + 增量
- **房主模式**：房主作为权威节点

## 依赖项（延续阶段1-7）

```kotlin
// 阶段1-7的所有依赖

// 额外依赖
implementation 'io.ktor:ktor-websockets'
implementation 'io.ktor:ktor-client-websockets'
implementation 'io.ktor:ktor-client-core'
```

## 里程碑检查清单

- [ ] 房间创建可用
- [ ] 房间加入可用
- [ ] 命令同步正确
- [ ] 状态同步正确
- [ ] 断线重连可用
